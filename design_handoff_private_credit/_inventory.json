{
  "source": "Private Credit.dc.html",
  "elements": [
    "heroCv",
    "p2Track",
    "p2Pin",
    "p2Wrap",
    "p2Stage",
    "p2Title",
    "p2Win",
    "p2Side",
    "p2Body",
    "p2Cap",
    "oTrack",
    "oPin",
    "oWrap",
    "oStage",
    "oCount",
    "oField",
    "oVig",
    "oHead",
    "oFit",
    "dTrack",
    "dPin",
    "dInner",
    "dStack",
    "dCta",
    "dC1",
    "dC2",
    "dC3",
    "dC4",
    "servHost",
    "lpHost",
    "lpHead",
    "lpFit",
    "iWrap",
    "iStage",
    "iMark",
    "rHead",
    "rFit",
    "p2Head",
    "p2Fit",
    "track",
    "pin",
    "wrap",
    "stage",
    "titleA",
    "titleB",
    "w0",
    "w1",
    "w2",
    "w3",
    "plat",
    "badgeA",
    "pfSide",
    "pfMain",
    "pfRail"
  ],
  "methods": {
    "core": [
      "constructor",
      "step",
      "arm",
      "watch",
      "boot",
      "ensure",
      "start",
      "setOptions",
      "destroy",
      "_teardown",
      "applyTheme",
      "accent",
      "cur",
      "cl",
      "seg",
      "ease",
      "eio",
      "lerp",
      "num",
      "inView",
      "secProg",
      "trackProg",
      "fit",
      "el",
      "monoF",
      "sansF",
      "money",
      "mono",
      "cropOf",
      "pinFit",
      "frame",
      "syncScroll"
    ],
    "03-origination": [
      "oLive",
      "oBuild",
      "oGates",
      "oSheet",
      "oCard",
      "oEqualize",
      "oSync"
    ],
    "hero": [
      "h2",
      "shade",
      "blob",
      "bristle",
      "ringPt",
      "drawHeroRing"
    ],
    "02-platform": [
      "p2Tone",
      "p2BuildSide",
      "p2Sync",
      "p2SetStress",
      "p2Reveal",
      "p2TypeInto",
      "p2Show",
      "p2Header",
      "p2RenderBody",
      "p2DealPanel",
      "p2PaneKanban",
      "p2PaneScore",
      "docIcon",
      "p2PaneMemo",
      "p2SetMemo",
      "p2PaneIC",
      "p2PaneWatch",
      "p2BuildDossier",
      "p2SetCerts",
      "p2SetDossier"
    ],
    "05-investors": [
      "lpFitNow",
      "buildLp",
      "loopLp",
      "stepLp"
    ],
    "04-servicing": [
      "buildServ",
      "loopServ",
      "stepServ"
    ],
    "06-delivery": [
      "dIcon",
      "dRP",
      "dPuck",
      "dBuild",
      "dLayout",
      "dSync"
    ],
    "intro": [
      "iClear",
      "maybeIntro",
      "iEl",
      "iFmt",
      "iTeardown",
      "skipIntro",
      "iLift",
      "iMarkIn",
      "runIntro"
    ],
    "01-reality": [
      "tone",
      "buildTitle",
      "revealWords",
      "win",
      "sync",
      "buildPlatform",
      "showScreen",
      "renderMain",
      "renderRail"
    ]
  },
  "imperativeStrings": [
    "heroCv",
    "p2Track",
    "p2Pin",
    "p2Wrap",
    "p2Stage",
    "p2Title",
    "p2Win",
    "p2Side",
    "p2Body",
    "p2Cap",
    "oTrack",
    "oPin",
    "oWrap",
    "oStage",
    "oCount",
    "oField",
    "oVig",
    "oHead",
    "oFit",
    "dTrack",
    "dPin",
    "dInner",
    "dStack",
    "dCta",
    "servHost",
    "lpHost",
    "lpHead",
    "lpFit",
    "iWrap",
    "iStage",
    "iMark",
    "rHead",
    "rFit",
    "p2Head",
    "p2Fit",
    "titleA",
    "titleB",
    "badgeA",
    "pfSide",
    "pfMain",
    "pfRail",
    "You currently operate on disconnected systems that never speak to each other",
    "3264 builds operational platforms that centralize everything in one system of record.",
    "record.",
    "PIPE",
    "CRED",
    "KYC",
    "SERV",
    "PORT",
    "COVT",
    "FUND",
    "INVR",
    "DATA",
    "Deal Pipeline",
    "Credit Files",
    "KYC & AML",
    "Loan Servicing",
    "Portfolio Monitoring",
    "Covenant Tracking",
    "Fund Accounting",
    "Investor Portal",
    "Data Room",
    "Pipeline",
    "List",
    "Declines",
    "Sources",
    "Opportunity",
    "Sponsor",
    "Size",
    "Stage",
    "Owner",
    "Halcyon Dental Group",
    "Bracken Partners",
    "Screening",
    "S. Devraj",
    "Northgate Cold Chain",
    "Ferris Capital",
    "Underwriting",
    "A. Whitfield",
    "Rowan Speciality Foods",
    "Term sheet",
    "M. Oyelaran",
    "Aldermoor Testing",
    "Vance Equity",
    "At committee",
    "Tarn Industrial",
    "H. Lindqvist",
    "Declines recorded with a reason · 96 screened this quarter",
    "Source",
    "4 deals with us to date",
    "This vintage",
    "Screened",
    "Committee agenda",
    "Source attribution",
    "Memo",
    "Comparables",
    "Diligence",
    "Metric",
    "Covenant",
    "Revenue",
    "EBITDA",
    "Net debt",
    "Total leverage",
    "Interest cover",
    "Spread from audited accounts · downside case holds at 4.41x",
    "Northgate_FY24_Audited.pdf",
    "received 04 Mar",
    "Proposed",
    "Hold",
    "Sponsor equity",
    "Risk rating",
    "Committee pack",
    "Rating history",
    "Documentation",
    "Agreement",
    "Security",
    "Signatures",
    "Condition",
    "Responsible",
    "Received",
    "Status",
    "Executed credit agreement",
    "Legal",
    "Debenture and share pledge",
    "Insurance certificates",
    "Borrower",
    "Legal opinion, jurisdiction",
    "Counsel",
    "Funds flow statement",
    "Fund operations",
    "Funding stays blocked until every condition is satisfied",
    "Aldermoor_CreditAgreement.pdf",
    "executed 18 Mar",
    "Terms",
    "Facility",
    "Margin",
    "Term",
    "terms read into the engine",
    "Funding memo",
    "Servicing schedule",
    "Security register",
    "Servicing",
    "Cash",
    "Fees",
    "Rate resets",
    "Line",
    "Rate",
    "Amount",
    "Treatment",
    "Cash interest",
    "PIK accrued",
    "Commitment fee, undrawn",
    "Scheduled amortisation",
    "Closing drawn balance",
    "Computed from the agreement, not maintained by hand",
    "SOFR 3M + 5.75%",
    "floor 1.00%, not binding",
    "Position",
    "Drawn",
    "Undrawn",
    "Next reset",
    "reset 31 Mar, applied",
    "General ledger",
    "Investor report",
    "Borrowing base",
    "Covenants",
    "Cash flows",
    "Documents",
    "Test",
    "Actual",
    "Headroom",
    "State",
    "Total net leverage",
    "Fixed charge cover",
    "Minimum liquidity",
    "Capex limit",
    "Tested 14 Mar 06:42 · source Ardyne_CompCert_Q3.pdf p.4 · no manual entry",
    "Source document",
    "Ardyne_CompCert_Q3.pdf",
    "received 14 Mar 06:40",
    "Extracted",
    "confidence 0.98 · p.4 row 118",
    "Watchlist",
    "Facility certificate",
    "Investors",
    "Calls",
    "Distributions",
    "Side letter",
    "Basis",
    "Reference",
    "Opening capital account",
    "Contributions",
    "call 07",
    "Net investment income",
    "Closing capital account",
    "ILPA-aligned · side letter and MFN terms applied, not remembered",
    "Investor",
    "Ashfield Pension Trust",
    "Class A · admitted 2024",
    "Commitment",
    "Called",
    "MFN elected",
    "fee variation per class",
    "Quarterly pack",
    "Capital account",
    "Fee calculation",
    "Reporting",
    "Regulatory",
    "Audit file",
    "Output",
    "Released",
    "Capital accounts, 18 investors",
    "Portfolio summary",
    "Asset coverage certificate",
    "Annex IV extract",
    "in review",
    "Valuation support file",
    "Every figure traces to the document and the test behind it",
    "One record",
    "24 positions · 40 tests",
    "This pack",
    "Outputs",
    "exportable for the annual review",
    "Facility agent",
    "Auditor",
    "Regulator",
    "Six stages, five asset classes, one view.",
    "one view.",
    "Pipeline / Q3 2026",
    "Live board",
    "38 deals · $1.24bn sourced",
    "factors,",
    "Base case and downside, side by side. The rating falls out of the numbers.",
    "The rating falls out of the numbers.",
    "Underwriting / Northgate Cold Chain",
    "Credit scorecard",
    "base case v3 · downside modelled",
    "Credit memo",
    "Every figure cites the document and page it came from.",
    "the document and page it came from.",
    "Credit memo / Northgate Cold Chain",
    "Investment memorandum",
    "draft 4 · 8 of 9 sections complete",
    "Committee",
    "Approved with conditions. Seventeen of them, each with an owner.",
    "each with an owner.",
    "Committee / 12 Mar sitting",
    "approved with conditions · 4 of 5",
    "Monitoring",
    "itself,",
    "24 positions tested on receipt. Two on watch, one breached, everyone already knows.",
    "everyone already knows.",
    "Monitoring / Portfolio",
    "24 positions · tested to 31 Mar",
    "Sourced",
    "Direct lending",
    "Asset-backed",
    "Vale Logistics",
    "Speciality",
    "Kelby Marine",
    "Bram Estates",
    "Real estate",
    "Pell NAV Facility",
    "NAV finance",
    "Funded",
    "Denholm Foods",
    "Leverage and headroom",
    "Transaction overview",
    "Business and market",
    "Financial analysis",
    "Structure and security",
    "Covenant package",
    "Key risks",
    "Recommendation",
    "Chair",
    "Head of credit",
    "Portfolio",
    "Risk",
    "R. Ashworth",
    "Independent",
    "Legal opinion",
    "Fund ops",
    "Sanctions re-screen",
    "Compliance",
    "Ardyne Packaging",
    "Brightmoor Health",
    "Calder Logistics",
    "Estree Dental",
    "Fenwick Marine",
    "Garrow Software",
    "deals seen this vintage",
    "Pipeline / intake · 06 Mar 09:12",
    "Halcyon Dental",
    "Introduced by",
    "Aldgate Partners",
    "Asset class",
    "Indicative ticket",
    "Attribution",
    "recorded on entry",
    "Every deal enters with its source, its broker and its owner attached",
    "through the policy gates",
    "Screening / policy gates",
    "Five gates, one fail",
    "Ticket size",
    "Sector",
    "dental roll-up · pass",
    "Opening leverage",
    "5.4x · above 4.75 policy",
    "Jurisdiction",
    "UK · pass",
    "Sponsor record",
    "first-time · flagged",
    "152 declined here, each against the gate that stopped it",
    "Underwritten",
    "spread and modelled",
    "Underwriting / spread · FY23A to FY25E",
    "The spread, cell by cell",
    "Net debt / EBITDA",
    "audited file, tied line by line",
    "One base case · the memo and the covenants both read it",
    "To committee",
    "papers written",
    "Credit memo / thirteen sections",
    "The memo, assembled",
    "Sections drafted",
    "approval, subject to conditions",
    "Papers out",
    "10 Mar, two days before the sitting",
    "Citations",
    "4 documents, page level",
    "Written from the diligence, not retyped from it",
    "Approved",
    "approved by committee",
    "Approved with conditions",
    "Votes",
    "Condition 15",
    "hedging within 60 days",
    "Condition 16",
    "capex basket at 6.0",
    "Minutes",
    "recorded at the sitting",
    "KYC, UBO and sanctions",
    "cleared 11 Mar",
    "The dissent became a condition, not a footnote",
    "positions live",
    "Closing / conditions precedent",
    "Seventeen conditions, all satisfied",
    "Security package",
    "debenture and share pledge",
    "Perfection",
    "registered 14 Mar",
    "Funds flow",
    "Position live",
    "The record",
    "continues into monitoring",
    "Eleven funded from two hundred and forty-eight seen",
    " + this._errs[name] + ",
    " + String((e && e.stack) || ",
    ").split(",
    ").slice(1, 3).join(",
    ", name, e);\n      }\n    }\n  }\n\n  // The clock re-arms itself: a hot-swapped logic class or a stray teardown can\n  // clear the timers without re-mounting, and rAF does not fire in some preview\n  // contexts, so an animation frame drives when it can and the timer covers it.\n  arm() {\n    this.ivals.forEach(clearInterval);\n    this.ivals = [];\n    this._beat = 0;\n    this._seen = -1;\n    this._hbAt = Date.now();\n    this.ivals.push(setInterval(() => {\n      if (this._beat === this._seen) this.frame();\n      this._seen = this._beat;\n    }, 34));\n    this.ivals.push(setInterval(() => {\n      this._hbAt = Date.now();\n      this.syncScroll();\n    }, 400));\n    if (this._raf) cancelAnimationFrame(this._raf);\n    const loop = () => {\n      this._beat++;\n      this.frame();\n      this._raf = requestAnimationFrame(loop);\n    };\n    this._raf = requestAnimationFrame(loop);\n  }\n\n  // A teardown can stop the clock without unmounting the tree, so the heartbeat is\n  // watched from window scope, which no component teardown can reach.\n  watch() {\n    window.__pcLive = this;\n    if (window.__pcWatch) return;\n    window.__pcWatch = setInterval(() => {\n      const c = window.__pcLive;\n      if (c && Date.now() - (c._hbAt || 0) > 1400) c.arm();\n    }, 900);\n  }\n\n  // Setup runs from renderVals as well as from mount: the logic class can be hot\n  // swapped into a live instance without componentDidMount ever running again, and\n  // the whole page is imperative, so nothing may assume the mount hook fired.\n  boot() {\n    if (this._boot) return;\n    this._boot = true;\n    if (!this.t0) this.t0 = performance.now();\n    if (!this._onScroll) {\n      this._onScroll = () => {\n        if (!this.ivals.length || Date.now() - (this._hbAt || 0) > 1400) this.arm();\n        this.ensure();\n      };\n      window.addEventListener(",
    ", this._onScroll, { passive: true });\n      window.addEventListener(",
    ", this._onScroll);\n    }\n    this.arm();\n    this.watch();\n    // microtask, not a timer: timers are frozen in some embedded contexts\n    Promise.resolve().then(() => this.ensure());\n  }\n\n  // idempotent: builds whatever refs have attached, then syncs\n  ensure() {\n    if (!this._introTried) { this._introTried = true; this.step(",
    "); }\n    if (!this._themed) { this._themed = true; this.step(",
    "); }\n    if (!this.p2NavRows) this.step(",
    ");\n    if (!this.servA) this.step(",
    ");\n    if (!this.lpRows) this.step(",
    ");\n    if (!this.oTiles) this.step(",
    ");\n    if (!this.dMods) this.step(",
    ");\n    this.syncScroll();\n  }\n\n  // PORT SHIM (2/4): public lifecycle. start() replaces componentDidMount; setOptions()\n  // replaces componentDidUpdate; destroy() replaces componentWillUnmount.\n  start() {\n    this.boot();\n  }\n  setOptions(opts) {\n    Object.assign(this.props, opts || {});\n    if (opts && ",
    " in opts) this.frozenT = opts.frozenT != null ? Number(opts.frozenT) : null;\n    this.applyTheme();\n    if (!this.ivals.length) this.arm();\n    this.ensure();\n  }\n  destroy() {\n    this._teardown();\n    if (window.__pcWatch && !window.__pcLive) { clearInterval(window.__pcWatch); window.__pcWatch = null; }\n    this._onScroll = null;\n    this._boot = false;\n  }\n  _teardown() {\n    this.iClear();\n    if (this._raf) cancelAnimationFrame(this._raf);\n    if (window.__pcLive === this) window.__pcLive = null;\n    if (this._iKey) window.removeEventListener(",
    ", this._onScroll);\n  }\n\n  applyTheme() {\n    const b = document.body;\n    if (!b) return;\n    b.style.setProperty(",
    ", this.props.accent ?? ",
    ", (this.props.gridTexture ?? true) ? ",
    ");\n  }\n  get accent() { return this.props.accent ?? ",
    "; }\n  get cur() { return this.props.currency ?? ",
    "; }\n\n  cl(x) { return x < 0 ? 0 : x > 1 ? 1 : x; }\n  seg(s, a, b) { return this.cl((s - a) / (b - a)); }\n  ease(x) { return 1 - Math.pow(1 - x, 3); }\n  eio(x) { return x < 0.5 ? 2 * x * x : 1 - Math.pow(-2 * x + 2, 2) / 2; }\n  lerp(a, b, t) { return a + (b - a) * t; }\n  num(n, d) { return n.toLocaleString(",
    ", { minimumFractionDigits: d || 0, maximumFractionDigits: d || 0 }); }\n\n  inView(el, m) {\n    if (!el) return false;\n    const r = el.getBoundingClientRect();\n    const pad = m || 0;\n    return r.bottom > -pad && r.top < window.innerHeight + pad;\n  }\n  secProg(el) {\n    if (!el) return 0;\n    const r = el.getBoundingClientRect();\n    const vh = window.innerHeight;\n    return this.cl((vh * 0.86 - r.top) / (vh * 0.52));\n  }\n  trackProg(track, pin) {\n    if (!track || !pin) return 0;\n    const span = track.offsetHeight - pin.offsetHeight;\n    if (span <= 0) return 0;\n    return this.cl(-track.getBoundingClientRect().top / span);\n  }\n  fit(ref, key) {\n    const cv = ref.current;\n    if (!cv) return null;\n    const r = cv.getBoundingClientRect();\n    if (r.width < 2 || r.height < 2) return null;\n    const d = Math.min(window.devicePixelRatio || 1, 2);\n    const s = this[key];\n    if (!s || Math.abs(s.w - r.width) > 0.5 || Math.abs(s.h - r.height) > 0.5 || s.d !== d) {\n      cv.width = Math.round(r.width * d);\n      cv.height = Math.round(r.height * d);\n      this[key] = { w: r.width, h: r.height, d: d };\n    }\n    const x = cv.getContext(",
    ");\n    x.setTransform(d, 0, 0, d, 0, 0);\n    return { x: x, w: this[key].w, h: this[key].h };\n  }\n  el(css, text) {\n    const n = document.createElement(",
    ");\n    n.style.cssText = css;\n    if (text !== undefined) n.textContent = text;\n    return n;\n  }\n  monoF(x, size) { x.font = '500 ' + (size || 9.5) + 'px ",
    "Helvetica Neue",
    ";\n      return (v < 0 && Math.abs(v) >= Math.pow(10, -d) / 2) ? ",
    " + mag + ",
    " : mag;\n    };\n  }\n  get mono() { return ",
    "; }\n  // a brighter sibling of the brand green, for the deals that hold\n  get oLive() { return this.shade(this.props.accent ?? ",
    ", 0.10); }\n\n  // ---- painterly helpers -------------------------------------------------\n  // Deterministic jitter: every stroke must land in the same place each frame,\n  // otherwise the paint boils instead of sitting on the canvas.\n  h2(a, b) {\n    const s = Math.sin(a * 127.1 + b * 311.7) * 43758.5453;\n    return s - Math.floor(s);\n  }\n  shade(hex, amt) {\n    const m = String(hex).replace(",
    ");\n    if (m.length !== 6) return hex;\n    const p = (i) => parseInt(m.slice(i, i + 2), 16);\n    const g = (v) => Math.max(0, Math.min(255, Math.round(v + amt * 255)));\n    return ",
    ";\n  }\n  blob(x, bx, by, r, seed) {\n    const n = 24;\n    x.beginPath();\n    for (let i = 0; i <= n; i++) {\n      const a = (i / n) * Math.PI * 2;\n      const wv = 1 + 0.065 * Math.sin(a * 3 + seed) + 0.045 * Math.sin(a * 5 - seed * 1.7)\n                   + 0.028 * Math.sin(a * 7 + seed * 0.6);\n      const px = bx + Math.cos(a) * r * wv, py = by + Math.sin(a) * r * wv;\n      if (i === 0) x.moveTo(px, py); else x.lineTo(px, py);\n    }\n    x.closePath();\n  }\n  bristle(x, p0, c, p1, o) {\n    const k = o.n || 3;\n    const dx = p1.x - p0.x, dy = p1.y - p0.y;\n    const L = Math.hypot(dx, dy) || 1;\n    const nx = -dy / L, ny = dx / L;\n    x.lineCap = ",
    ";\n    for (let s = 0; s < k; s++) {\n      const r1 = this.h2(o.seed, s * 3.1), r2 = this.h2(o.seed + 5.3, s * 7.7);\n      const off = (s - (k - 1) / 2) * (o.spread || 1.7) + (r1 - 0.5) * 1.1;\n      x.strokeStyle = o.colours[s % o.colours.length];\n      x.globalAlpha = o.alpha * (0.5 + 0.5 * r2);\n      x.lineWidth = (o.width || 1.4) * (0.65 + 0.7 * r1);\n      x.beginPath();\n      x.moveTo(p0.x + nx * off, p0.y + ny * off);\n      x.quadraticCurveTo(c.x + nx * off * 1.7, c.y + ny * off * 1.7, p1.x + nx * off, p1.y + ny * off);\n      x.stroke();\n    }\n  }\n  ringPt(cx, cy, rad, i, n) {\n    const a = -Math.PI / 2 + (i / n) * Math.PI * 2;\n    return { x: cx + Math.cos(a) * rad, y: cy + Math.sin(a) * rad, a: a };\n  }\n\n  drawHeroRing(t) {\n    const fd = this.fit(this.heroCv, ",
    ");\n    if (!fd) return;\n    const x = fd.x, w = fd.w, h = fd.h;\n    const T = 27, c = t % T;\n    const n = this.sysO.length;\n    const H = h;\n    const ink = ",
    ";\n    const acc = this.accent;\n\n    x.globalAlpha = 1;\n    x.clearRect(0, 0, w, h);\n\n    this.sansF(x, 12);\n    const cx = w / 2;\n    const nodeR = 17, gap = 11, pad = 6, lineH = 15;\n    let cy = H / 2 + 2;\n    // Solve the radius per angle rather than reserving the widest label in every\n    // direction: only nodes near the horizontal axis need the full label width,\n    // and nodes near the vertical axis need almost none.\n    let radX = w, radY = H;\n    for (let i = 0; i < n; i++) {\n      const a = -Math.PI / 2 + (i / n) * Math.PI * 2;\n      const ac = Math.abs(Math.cos(a)), as = Math.abs(Math.sin(a));\n      const side = ac > 0.25;\n      const lw = x.measureText(this.fullO[i]).width;\n      const hx = side ? (nodeR + gap + lw) : (lw / 2);\n      if (ac > 0.001) radX = Math.min(radX, (w / 2 - pad - hx) / ac);\n      const vy = side ? Math.max(nodeR, lineH / 2) : (nodeR + gap + lineH);\n      if (as > 0.001) radY = Math.min(radY, (H / 2 - pad - vy) / as);\n    }\n    radX = Math.max(56, radX);\n    // Labels reach sideways, so the horizontal bound is much tighter than the\n    // vertical. A slight ellipse claims that spare height; capped so it still\n    // reads as a ring rather than an oval.\n    radY = Math.max(56, Math.min(radY, radX * 1.16));\n    // Sit the ring hard against the top of its frame: the eye reads space above\n    // a diagram as emptiness, so all slack is pushed below the bottom labels.\n    // Reserve the top label's full glyph box: it is drawn on a ",
    " baseline,\n    // so it extends upward from its anchor and the canvas clips at its own edge.\n    cy = Math.min(cy, radY + nodeR + gap + lineH + 1);\n\n    const appear = this.ease(this.cl(c / 2.2));\n    const mesh = this.seg(c, 2.4, 6.6);\n    const hold = this.seg(c, 6.6, 8.2);\n    const fold = this.eio(this.seg(c, 8.2, 11.4));\n    const clean = this.seg(c, 11.4, 13.2);\n    const out = this.seg(c, 26.4, 27);\n\n    const greens = [this.shade(acc, -0.04), acc, this.shade(acc, 0.1)];\n    const greys = [",
    " + (0.24 * k).toFixed(3) + ",
    ";\n        x.lineWidth = 1;\n        x.setLineDash([3, 4]);\n        x.lineDashOffset = -t * 14;\n        x.beginPath();\n        x.moveTo(a.x, a.y);\n        x.quadraticCurveTo(ctl.x, ctl.y, b.x, b.y);\n        x.stroke();\n        x.setLineDash([]);\n        if (k >= 1) {\n          const ph = ((t * 0.22 + i * 0.37) % 1);\n          const iu = 1 - ph;\n          x.fillStyle = ",
    " + ink + ",
    ";\n          x.globalAlpha = (1 - out) * Math.sin(ph * Math.PI) * 0.26;\n          x.beginPath();\n          x.arc(iu * iu * a.x + 2 * iu * ph * ctl.x + ph * ph * b.x,\n                iu * iu * a.y + 2 * iu * ph * ctl.y + ph * ph * b.y, 1.9, 0, Math.PI * 2);\n          x.fill();\n          x.globalAlpha = 1 - out;\n        }\n      });\n    }\n\n    if (fold > 0) {\n      const cr = 29 * fold;\n      for (let i = 0; i < n; i++) {\n        const a = pt(i);\n        x.strokeStyle = acc;\n        x.globalAlpha = (1 - out) * fold * 0.6;\n        x.lineWidth = 1.5;\n        x.beginPath();\n        x.moveTo(a.x, a.y);\n        x.lineTo(this.lerp(a.x, cx, fold), this.lerp(a.y, cy, fold));\n        x.stroke();\n        x.globalAlpha = 1 - out;\n      }\n      if (clean > 0) {\n        for (let i = 0; i < n; i++) {\n          const a = pt(i);\n          const ph = ((t * 0.55 + i / n) % 1);\n          x.fillStyle = acc;\n          x.globalAlpha = (1 - out) * Math.sin(ph * Math.PI) * 0.85;\n          x.beginPath();\n          x.arc(this.lerp(a.x, cx, ph), this.lerp(a.y, cy, ph), 2.6, 0, Math.PI * 2);\n          x.fill();\n          x.globalAlpha = 1 - out;\n        }\n      }\n      x.fillStyle = ",
    ";\n      x.beginPath(); x.arc(cx, cy, cr, 0, Math.PI * 2); x.fill();\n      x.strokeStyle = acc;\n      x.lineWidth = 1.8;\n      x.beginPath(); x.arc(cx, cy, cr, 0, Math.PI * 2); x.stroke();\n      const lab = this.cl((fold - 0.55) / 0.35);\n      if (lab > 0) {\n        x.globalAlpha = (1 - out) * lab;\n        x.font = '500 16px ",
    "letterSpacing",
    "var(--a)",
    "var(--warn)",
    "var(--bad)",
    " + this.mono + ",
    "Meridian Credit II",
    "min-width:0",
    "margin:auto 16px 0; padding-top:14px; border-top:1px solid rgba(20,20,18,0.08); ",
    "one record",
    "five asset classes",
    "; }\n  }\n\n  // Section 05 is a static vignette: scale it so the headline and the card\n  // land on one screen together.\n  lpFitNow() {\n    const host = this.lpHost.current, box = this.lpFit.current, head = this.lpHead.current;\n    if (!host || !box || !head) return;\n    const card = host.firstElementChild;\n    if (!card) return;\n    const nat = card.offsetHeight;\n    if (!nat) return;\n    const key = window.innerHeight + ",
    " + head.offsetHeight + ",
    " + nat;\n    if (this._lpKey === key) return;\n    this._lpKey = key;\n    const mt = parseFloat(getComputedStyle(box).marginTop) || 0;\n    const avail = window.innerHeight - 68 - head.offsetHeight - mt - 28;\n    const k = Math.max(0.62, Math.min(1, avail / nat));\n    host.style.transform = k < 0.999 ? ",
    " + k.toFixed(4) + ",
    ";\n  }\n\n  // ---- scroll ------------------------------------------------------------\n  p2Sync() {\n    const track = this.p2Track.current, pin = this.p2Pin.current, wrap = this.p2Wrap.current, stage = this.p2Stage.current;\n    if (!track || !pin) return;\n    this.pinFit(this.p2Fit, this.p2Wrap, 1200, 780);\n    const p2Crop = this.cropOf(this.p2Wrap, 1200, 780);\n    if (this.p2Cap.current && this.p2Cap.current.dataset.crop !== String(p2Crop)) {\n      this.p2Cap.current.dataset.crop = String(p2Crop);\n      this.p2Cap.current.style.bottom = (36 + p2Crop) + ",
    ";\n    }\n    if (this.p2Side.current && this.p2Side.current.children.length === 0) this.p2BuildSide();\n\n    if (wrap && stage) {\n      const w = wrap.getBoundingClientRect().width;\n      if (w) {\n        const k = Math.min(1, w / 1200).toFixed(4);\n        if (stage.dataset.k !== k) { stage.dataset.k = k; stage.style.transform = ",
    "; }\n      }\n    }\n\n    const span = track.offsetHeight - pin.offsetHeight;\n    const p = span > 0 ? this.cl(-track.getBoundingClientRect().top / span) : 0;\n\n    const pWin = this.seg(p, 0.04, 0.11);\n    if (this.p2Win.current) {\n      const k = this.ease(pWin);\n      this.p2Win.current.style.opacity = String(k);\n      this.p2Win.current.style.transform = ",
    " + ((1 - k) * 26).toFixed(2) + ",
    ";\n    }\n\n    // five equal blocks; each reveals its line over the first third, then holds\n    const n = this.p2Stages.length;\n    const walk = this.seg(p, 0.10, 0.99) * n;\n    const idx = Math.min(n - 1, Math.floor(walk));\n    const local = this.cl(walk - idx);\n    if (idx !== this.p2At) this.p2Show(idx);\n    this.p2Reveal(this.p2Words, this.cl(local / 0.30));\n    if (idx === 2 && this.p2MemoBridge) this.p2SetMemo(local);\n    if (idx === 1 && this.p2StressBars) this.p2SetStress(this.ease(this.seg(local, 0.34, 0.78)));\n    if (idx === 4 && this.p2CertRows) {\n      this.p2SetCerts(this.seg(local, 0.28, 0.58));\n      this.p2SetDossier(this.ease(this.seg(local, 0.62, 0.90)));\n    }\n    if (idx === 0 && this.p2Deal) {\n      const open = this.ease(this.seg(local, 0.36, 0.68));\n      this.p2Deal.style.opacity = String(open);\n      this.p2Deal.style.transform = ",
    " + (0.972 + 0.028 * open).toFixed(4) + ",
    ";\n      if (this.p2BoardCols) this.p2BoardCols.forEach((c) => { c.style.opacity = String(1 - open * 0.72); });\n    }\n    this.p2Reveal(this.p2CapWords, this.cl((local - 0.16) / 0.30));\n  }\n\n  // 0 = sponsor's base case, 1 = EBITDA down 20% and the reference rate up 200bps\n  p2SetStress(s) {\n    const MAX = 5.0, H = 132;\n    if (this.p2StressBars) {\n      this.p2StressBars.forEach((b) => {\n        const v = b.base + (b.down - b.base) * s;\n        b.bar.style.height = ((v / MAX) * (H - 16)).toFixed(1) + ",
    ";\n        b.bar.style.background = v > 4.4 ? ",
    ";\n      });\n    }\n    const peak = 4.19 + (4.41 - 4.19) * s;\n    if (this.p2StressPeak) this.p2StressPeak.textContent = peak.toFixed(2) + ",
    ";\n    if (this.p2StressHead) {\n      this.p2StressHead.textContent = (4.75 - peak).toFixed(2) + ",
    ";\n    }\n    if (this.p2StressRating) {\n      this.p2StressRating.textContent = s > 0.72 ? ",
    ";\n    }\n    if (this.p2StressLabel) {\n      this.p2StressLabel.textContent = s < 0.08 ? ",
    ";\n      this.p2StressLabel.style.color = s < 0.08 ? ",
    ";\n    }\n    if (this.p2StressToast) {\n      const show = this.cl(s / 0.06);\n      this.p2StressToast.style.opacity = String(show);\n      this.p2StressToast.style.transform = ",
    " + ((1 - show) * -6).toFixed(1) + ",
    ";\n      const done = s > 0.97;\n      const col = done ? ",
    ";\n      this.p2ToastBar.style.background = col;\n      this.p2ToastDot.style.background = col;\n      this.p2ToastPct.textContent = done ? ",
    ";\n      const ti = done ? ",
    ";\n      if (this.p2ToastTitle.textContent !== ti) this.p2ToastTitle.textContent = ti;\n    }\n    if (this.p2StressNote) {\n      this.p2StressNote.textContent = s > 0.9\n        ? ",
    ";\n    }\n  }\n\n  p2Reveal(spans, prog) {\n    if (!spans) return;\n    const n = spans.length;\n    spans.forEach((s, i) => {\n      const k = this.ease(this.cl(prog * (n + 3) - i));\n      s.style.opacity = String(k);\n      s.style.transform = ",
    " + ((1 - k) * 13).toFixed(2) + ",
    ";\n    });\n  }\n\n  p2TypeInto(ref, text, emph, size) {\n    const el = ref.current;\n    if (!el) return [];\n    el.textContent = ",
    ";\n    const hot = (emph || ",
    ");\n    return text.split(",
    ").map((word, i, arr) => {\n      const s = document.createElement(",
    ");\n      s.textContent = word + (i === arr.length - 1 ? ",
    ");\n      const isHot = hot.indexOf(word) >= 0;\n      s.style.cssText = ",
    " +\n        (isHot ? ",
    ") + (isHot ? ",
    ");\n      el.appendChild(s);\n      return s;\n    });\n  }\n\n  p2Show(i) {\n    const s = this.p2Stages[i];\n    if (!s) return;\n    this.p2At = i;\n    if (this.p2NavRows) {\n      this.p2NavRows.forEach((r, k) => {\n        const on = k === i;\n        r.row.style.background = on ? ",
    ";\n        r.row.style.borderLeftColor = on ? ",
    ";\n        r.nm.style.color = on ? ",
    ";\n        r.ct.style.color = on ? ",
    "));\n    this.p2CapWords = this.p2TypeInto(this.p2Cap, s.cap, s.capEmph);\n    this.p2RenderBody(s);\n  }\n\n  p2Header(s) {\n    const bar = this.el(",
    ", s.crumb));\n    const row = this.el(",
    ", s.meta));\n    bar.appendChild(row);\n    return bar;\n  }\n\n  p2RenderBody(s) {\n    const h = this.p2Body.current;\n    if (!h) return;\n    h.textContent = ",
    ";\n    h.appendChild(this.p2Header(s));\n    const pane = this.el(",
    ");\n    h.appendChild(pane);\n    if (s.kind === ",
    ") this.p2PaneKanban(pane);\n    else if (s.kind === ",
    ") this.p2PaneScore(pane);\n    else if (s.kind === ",
    ") this.p2PaneMemo(pane);\n    else if (s.kind === ",
    ") this.p2PaneIC(pane);\n    else this.p2PaneWatch(pane);\n    h.animate([{ opacity: 0, transform: ",
    " });\n  }\n\n  // ---- 1 pipeline: a board built for asset classes -----------------------\n  // Scroll past the board and one deal opens: the specifics a deal team argues over.\n  p2DealPanel(pane) {\n    const p = this.el(",
    ");\n    const hd = this.el(",
    ");\n    const lt = this.el(",
    "Direct lending · unitranche · Ferris Capital",
    "hold 65.0 max",
    "1px solid rgba(20,20,18,0.05)",
    "SOFR 3M",
    "Cash margin",
    "PIK",
    "OID",
    "Floor",
    "Credit",
    "EV / EBITDA",
    "Attach / detach",
    "Sources and uses",
    "Refinance",
    "Acquisition",
    "Fees and OID",
    "Total uses",
    "Cash at close",
    "Underwriting, day 9",
    "Next",
    "Committee 12 Mar",
    "Last activity",
    "Site visit 06 Mar",
    "padding-bottom:7px; border-bottom:1px solid rgba(20,20,18,0.1)",
    "padding:8px 9px; background:#FFFFFF; border:1px solid rgba(20,20,18,0.11); border-radius:5px; border-left:2px solid ",
    "var(--mut)",
    "flex:",
    "), r[1]));\n        c.appendChild(row);\n      });\n      return c;\n    };\n    const left = this.el(",
    ");\n    const band = this.el(",
    "Recommended",
    "from B",
    "Delegated authority",
    "Hold at close",
    "Single name",
    "Sector, transport",
    "Sub-investment grade",
    "B+ / B− floor",
    "Routing",
    "IC, size only",
    "Within credit authority. Referred to committee on ticket size alone.",
    "flex:1 1 0; min-width:0; padding:0 18px; border-left:1px solid rgba(20,20,18,0.08); border-right:1px solid rgba(20,20,18,0.08)",
    "Risks and mitigants",
    "Customer concentration",
    "Top 3 at 46% of revenue",
    "Contracted to 2029, change-of-control consent",
    "Capex intensity",
    "Depot fit-out 8.2 over two years",
    "Capex basket capped, sponsor equity cure",
    "Rate exposure",
    "Unhedged at close",
    "Hedging covenant, 70% within 90 days",
    "Sponsor concentration",
    "Third Ferris deal in the fund",
    "Sponsor cap 15%, currently 11.4%",
    ", r[1]));\n      const m = this.el(",
    "base case",
    "px; height:0; border-top:1.5px dashed rgba(20,20,18,0.45)",
    "covenant 4.75x",
    "margin:14px 0 0; padding-top:12px; border-top:1px solid rgba(20,20,18,0.08)",
    "Peak leverage",
    "Headroom to covenant",
    "Indicative rating",
    ");\n    right.appendChild(this.p2StressNote);\n    // Sits in the window header, clear of the chart. One line, a hairline rule and\n    // a percentage: enough to explain the movement without becoming a second panel.\n    const host = this.p2Body.current;\n    if (host) {\n      host.style.position = ",
    ";\n      const toast = this.el(",
    ");\n      const row = this.el(",
    "Building stress scenario",
    ");\n      row.appendChild(this.p2ToastPct);\n      toast.appendChild(row);\n      const tt = this.el(",
    ");\n      tt.appendChild(this.p2ToastBar);\n      toast.appendChild(tt);\n      host.appendChild(toast);\n      this.p2StressToast = toast;\n    }\n    this.p2SetStress(0);\n    pane.appendChild(right);\n  }\n\n  // ---- 3 credit memo: the CAM, section by section, sources as documents ----\n  docIcon(name, ext) {\n    const row = this.el(",
    ");\n    const ic = this.el(",
    " + (ext === ",
    ", name));\n    return row;\n  }\n\n  p2PaneMemo(pane) {\n    const SECTS = [[",
    ", 0]];\n    const rail = this.el(",
    ");\n    const rh = this.el(",
    "Credit application",
    "1px solid rgba(20,20,18,0.04)",
    "));\n      const dot = document.createElement(",
    ";\n      row.appendChild(dot);\n      const lab = this.el(",
    ", m[0]);\n      row.appendChild(lab);\n      rail.appendChild(row);\n      return { dot: dot, lab: lab, done: m[1] };\n    });\n    pane.appendChild(rail);\n\n    const doc = this.el(",
    ");\n    pane.appendChild(doc);\n\n    // ================= screen S: executive summary =======================\n    // The page a committee member actually reads. Each claim pulls its figure\n    // from the section that produced it, so the summary is never retyped.\n    const S = this.el(",
    "1. Executive summary · 1.1 The request and our view",
    "Tenor",
    "Final hold",
    "62.5 of 65.0 max",
    "Ferris Capital seeks a unitranche of ",
    " to refinance existing debt and fund one bolt-on acquisition.",
    "We underwrite adjusted EBITDA of ",
    ", not the ",
    " in the sponsor's model.",
    "Opening leverage of ",
    " }\n    ];\n    const list = this.el(",
    ");\n    this.sumLines = LINES.map((L, i) => {\n      const row = this.el(",
    "));\n      const txt = this.el(",
    ");\n      const slots = [];\n      L.parts.forEach((p) => {\n        if (typeof p === ",
    ") { txt.appendChild(document.createTextNode(p)); return; }\n        const sp = document.createElement(",
    " + this.mono +\n          ",
    ", t: p.t });\n      });\n      row.appendChild(txt);\n      const tag = this.el(this.mono + ",
    ", L.tag);\n      row.appendChild(tag);\n      list.appendChild(row);\n      return { txt: txt, slots: slots, tag: tag };\n    });\n    S.appendChild(list);\n\n    const viewRow = this.el(",
    "We recommend approval, subject to conditions.",
    "flex:0 0 auto",
    "Documents cited",
    "Figures traced",
    "Each figure is the one in its section, not a retyped copy",
    "4. Business and market · 4.3 Customer concentration",
    "The top three customers are 46% of revenue. All three are contracted beyond facility maturity, with change-of-control consent obtained at close.",
    "Greaves Retail",
    "Halloran Foods",
    "Meridian Grocers",
    "Tarn Convenience",
    "Byre Wholesale",
    "Other, 34 accounts",
    "); border-right:1px solid #FFFFFF",
    "Contract expiry against facility maturity",
    "%; top:6px; transform:translateX(-50%) scale(0); width:9px; height:9px; border-radius:50%; background:#FFFFFF; border:1.5px solid rgba(20,20,18,0.6)",
    "Weighted average contract length",
    ", String(c[2])));\n    });\n    A.appendChild(tbl);\n    doc.appendChild(A);\n    this.memoA = A;\n\n    // ================= screen B: financial analysis ======================\n    const B = this.el(",
    "6. Financial analysis · 6.2 Earnings quality",
    "Adjustments total 1.8, of which 1.1 is non-recurring integration cost we accept and 0.7 is a run-rate synergy we do not.",
    "; outline:1px dashed rgba(20,20,18,0.34); outline-offset:-1px",
    "));\n        c.appendChild(d);\n        return { el: d };\n      }).reverse();\n      c.appendChild(this.el(this.mono + ",
    ", label));\n      chart.appendChild(c);\n      return segs;\n    };\n    const R = col(",
    " }]);\n    const P = col(",
    " }]);\n    const X = col(",
    ", dash: true }]);\n    const O = col(",
    ");\n    chart.appendChild(this.strike);\n    B.appendChild(chart);\n    this.p2MemoBridge = { H: H, MAX: MAX, R: R, P: P, X: X, O: O, chart: chart };\n\n    const out = this.el(",
    ");\n    const mk = (label) => {\n      const c = this.el(",
    ", label));\n      const v = this.el(",
    ");\n      c.appendChild(v);\n      out.appendChild(c);\n      return v;\n    };\n    this.memoEbitda = mk(",
    ");\n    B.appendChild(out);\n\n    const sh = this.el(",
    "Sources cited",
    "Northgate_FY24_Audited.pdf · p.12",
    "QoE_Report_Grant_Feb26.pdf · p.31",
    "Management_Budget_FY25.xlsx",
    "Depot_Utilisation_Q2.xlsx",
    "9. Covenant package · 9.2 Terms and protections",
    "Sixteen terms across four families. The marker shows where each sits against comparable deals.",
    "Financial maintenance",
    "10.0 at all times",
    "Negative covenants and baskets",
    "Incremental debt",
    "Permitted acquisitions",
    "5.0 per deal, 12.5 cap",
    "Restricted payments",
    "blocked above 3.50x",
    "Asset disposals",
    "2.5 de minimis, 12m reinvest",
    "Information undertakings",
    "Management accounts",
    "monthly, 30 days",
    "Compliance certificate",
    "quarterly, 45 days",
    "Audited accounts",
    "annual, 120 days",
    "Budget and board pack",
    "30 days pre year end",
    "Structural protections",
    "Equity cure",
    "Excess cash flow sweep",
    "50% above 3.50x",
    "Call protection",
    "NC1, then 102 / 101",
    "Anti-layering",
    "full blockers",
    "));\n        row.appendChild(pip);\n        blk.appendChild(row);\n        this.covRowsUI.push(row);\n      });\n      grid.appendChild(blk);\n    });\n    C.appendChild(grid);\n\n    const cf = this.el(",
    ");\n    const cmk = (label) => {\n      const c = this.el(",
    "Tighter than market",
    "Documentation strength",
    "Feeds the documentation factor in the scorecard",
    "\\u25aa tighter",
    "\\u25aa at market",
    "\\u25aa sponsor friendly",
    ", t)));\n    C.appendChild(scale);\n    doc.appendChild(C);\n    this.memoC = C;\n\n    // ================= screen D: risks and mitigants =====================\n    const D = this.el(",
    "12. Risks and mitigants · 12.1 Register and residual view",
    "Each risk carries the mitigant that reduces it. The marker moves as the mitigant attaches, so what remains is the residual view.",
    "Mitigant",
    "Residual",
    "), t)));\n    D.appendChild(hdr);\n\n    const RISKS = [\n      [",
    "Top three are 46% of revenue",
    "Contracted beyond maturity, change-of-control consent taken at close",
    "Financial",
    "Earnings quality",
    "0.7 of sponsor addbacks unsupported",
    "Addbacks capped at 15%; covenants set on our adjusted figure",
    "Floating and unhedged at close",
    "Hedging covenant, 70% of drawn within 60 days",
    "Execution",
    "Capex programme",
    "8.2 of depot fit-out over two years",
    "Basket capped at 6.0, equity cure available once",
    "Structural",
    "Third Ferris deal, 11.4% of the fund",
    "Hard sponsor cap of 15%, tested at every drawdown",
    "ESG",
    "Refrigerant compliance",
    "F-gas phase-down hits legacy units",
    "Replacement funded in the base case, annual ESG reporting",
    "Identified",
    "Reduced",
    "Held",
    "Residual medium on sponsor concentration, accepted against a hard 15% cap",
    "\\u25aa low",
    "\\u25aa medium",
    "\\u25aa high",
    ", t)));\n    D.appendChild(dscale);\n    doc.appendChild(D);\n    this.memoD = D;\n\n    // ================= screen E: recommendation ==========================\n    const E = this.el(",
    "13. Recommendation · 13.1 Proposal and sign-off",
    "margin:9px 0 0; padding:12px 14px; background:#F2EFE8; border:1px solid rgba(20,20,18,0.16); border-radius:5px; opacity:0",
    "Recommend approval, subject to conditions",
    ");\n    band.appendChild(this.recSub);\n    E.appendChild(band);\n    this.recBand = band;\n\n    const cols = this.el(",
    ");\n    const stackCol = (head) => {\n      const c = this.el(",
    ", head));\n      cols.appendChild(c);\n      return c;\n    };\n    const askCol = stackCol(",
    "Pricing",
    "S+575 cash, 200 PIK",
    ", r[1]));\n      askCol.appendChild(row);\n      return row;\n    });\n\n    const retCol = stackCol(",
    ", 1]].map((r, i) => {\n      const row = this.el(",
    ", r[0]));\n      const v = this.el(this.mono + ",
    "Why we recommend it",
    "Contracted revenue covers debt service 2.4x through the facility",
    "Covenants set on our adjusted EBITDA, not the sponsor's model",
    "Downside case holds inside covenant with 0.34x of cushion",
    "Sponsor has funded two prior deals through difficulty",
    "Ten of sixteen documentation terms tighter than market",
    ", t));\n      whyCol.appendChild(row);\n      return row;\n    });\n    E.appendChild(cols);\n\n    const sign = this.el(",
    "Investment team · 09 Mar",
    "Reviewed",
    "Head of credit · 10 Mar",
    "For decision",
    "Investment Committee",
    ");\n      c.appendChild(rule);\n      const nm = this.el(",
    ", s[1]);\n      c.appendChild(nm);\n      const mt = this.el(this.mono + ",
    ", s[2]);\n      c.appendChild(mt);\n      sign.appendChild(c);\n      return { rule: rule, nm: nm, mt: mt };\n    });\n    E.appendChild(sign);\n    doc.appendChild(E);\n    this.memoE = E;\n    this.p2SetMemo(0);\n  }\n\n  // One stage, six sections of the application: the summary that pulls from all of\n  // them, then business and market, earnings quality, covenants, risks, and the ask.\n  p2SetMemo(local) {\n    const pS = this.ease(this.cl(local / 0.13));\n    const sw1 = this.cl((local - 0.155) / 0.03);\n    const pA = this.ease(this.cl((local - 0.19) / 0.11));\n    const sw2 = this.cl((local - 0.325) / 0.03);\n    const br = this.ease(this.cl((local - 0.36) / 0.11));\n    const ct = this.ease(this.cl((local - 0.475) / 0.035));\n    const sw3 = this.cl((local - 0.525) / 0.03);\n    const pC = this.ease(this.cl((local - 0.56) / 0.11));\n    const sw4 = this.cl((local - 0.695) / 0.03);\n    const pD = this.ease(this.cl((local - 0.73) / 0.11));\n    const sw5 = this.cl((local - 0.855) / 0.03);\n    const pE = this.ease(this.cl((local - 0.89) / 0.11));\n\n    if (this.memoS) this.memoS.style.opacity = String(1 - sw1);\n    if (this.memoA) this.memoA.style.opacity = String(sw1 * (1 - sw2));\n    if (this.memoB) this.memoB.style.opacity = String(sw2 * (1 - sw3));\n    if (this.memoC) this.memoC.style.opacity = String(sw3 * (1 - sw4));\n    if (this.memoD) this.memoD.style.opacity = String(sw4 * (1 - sw5));\n    if (this.memoE) this.memoE.style.opacity = String(sw5);\n    if (this.railRows) {\n      const on = sw5 > 0.5 ? 12 : sw4 > 0.5 ? 11 : sw3 > 0.5 ? 8 : sw2 > 0.5 ? 5 : sw1 > 0.5 ? 3 : 0;\n      this.railRows.forEach((r, i) => {\n        const act = i === on;\n        r.dot.style.background = ",
    " + (act ? ",
    ";\n        r.lab.style.color = act ? ",
    ";\n      });\n    }\n\n    // The request lands, then each claim arrives and its figure drops into the slot\n    // from the section that produced it.\n    if (this.sumLines) {\n      const rq = this.cl(pS / 0.24);\n      if (this.sumReq) this.sumReq.forEach((v, i) => {\n        v.style.opacity = String(this.ease(this.cl(rq * (this.sumReq.length + 2) - i)));\n      });\n      const n = this.sumLines.length;\n      const ln = this.cl((pS - 0.18) / 0.56);\n      this.sumLines.forEach((L, i) => {\n        const k = this.ease(this.cl(ln * (n + 2) - i));\n        L.txt.style.opacity = String(k);\n        const kf = this.cl(k * 2.2 - 1.2);\n        L.slots.forEach((s) => {\n          if (s.t !== undefined) {\n            s.el.textContent = s.t;\n            s.el.style.opacity = String(kf);\n          } else {\n            s.el.textContent = kf > 0.02 ? (s.n * this.ease(kf)).toFixed(s.dp) + s.suf : ",
    " + (0.4 * (1 - kf)).toFixed(3) + ",
    ";\n        });\n        L.tag.style.opacity = String(this.cl(k * 2.4 - 1.5));\n      });\n      const f = this.cl((pS - 0.74) / 0.26);\n      if (this.sumView) this.sumView.style.opacity = String(this.ease(this.cl(f / 0.5)));\n      if (this.sumSects) this.sumSects.textContent = f > 0.05 ? Math.round(11 * f) + ",
    ";\n      if (this.sumDocs) this.sumDocs.textContent = f > 0.2 ? String(Math.round(4 * f)) : ",
    ";\n      if (this.sumTraced) this.sumTraced.textContent = f > 0.35 ? Math.round(6 * f) + ",
    ";\n      if (this.sumNote) this.sumNote.style.opacity = String(this.cl((f - 0.6) / 0.4));\n    }\n\n    // The ask and the returns, then the reasons, then the sign-off rules draw.\n    if (this.retRows) {\n      const e1 = this.cl(pE / 0.30), e2 = this.cl((pE - 0.26) / 0.30);\n      const e3 = this.cl((pE - 0.50) / 0.24), e4 = this.cl((pE - 0.72) / 0.28);\n      if (this.recBand) {\n        this.recBand.style.opacity = String(this.ease(this.cl(pE / 0.14)));\n        if (this.recSub) this.recSub.textContent = e3 > 0.4 ? ",
    ";\n      }\n      this.askRows.forEach((r, i) => {\n        const k = this.ease(this.cl(e1 * (this.askRows.length + 2) - i));\n        r.style.opacity = String(k);\n      });\n      this.retRows.forEach((r, i) => {\n        const k = this.cl(e1 * (this.retRows.length + 2) - i);\n        r.v.textContent = k > 0.02 ? (r.to * this.ease(k)).toFixed(r.dp) + r.suf : ",
    ";\n      });\n      this.whyRows.forEach((r, i) => {\n        const k = this.ease(this.cl(e2 * (this.whyRows.length + 2) - i));\n        r.style.opacity = String(k);\n        r.style.transform = ",
    " + ((1 - k) * 5).toFixed(1) + ",
    ";\n      });\n      this.signRows.forEach((r, i) => {\n        const k = this.ease(this.cl(e4 * (this.signRows.length + 1) - i));\n        r.rule.style.width = (k * 100).toFixed(1) + ",
    ";\n        r.nm.style.opacity = String(this.cl(k * 1.6 - 0.6));\n        r.mt.style.opacity = String(this.cl(k * 1.6 - 0.8));\n      });\n    }\n\n    // Risks land first, then the mitigants attach, then the marker slides from\n    // inherent to residual: the movement is the mitigation.\n    if (this.riskRows) {\n      const n = this.riskRows.length;\n      const r1 = this.cl(pD / 0.42), r2 = this.cl((pD - 0.38) / 0.32), r3 = this.cl((pD - 0.62) / 0.28);\n      this.riskRows.forEach((r, i) => {\n        const k1 = this.ease(this.cl(r1 * (n + 2) - i));\n        r.lt.style.opacity = String(k1);\n        r.pipWrap.style.opacity = String(k1);\n        const k2 = this.ease(this.cl(r2 * (n + 2) - i));\n        r.mt.style.opacity = String(k2);\n        r.mt.style.transform = ",
    " + ((1 - k2) * -7).toFixed(1) + ",
    ";\n        const k3 = this.ease(this.cl(r3 * (n + 2) - i));\n        const pos = r.inh + (r.res - r.inh) * k3;\n        r.cells.forEach((c, j) => {\n          const d = Math.max(0, 1 - Math.abs(j - pos));\n          c.style.background = ",
    " + (0.11 + 0.67 * d).toFixed(3) + ",
    ";\n        });\n      });\n      const fin = this.cl((pD - 0.86) / 0.14);\n      if (this.riskCount) this.riskCount.textContent = fin > 0.03 ? ",
    ";\n      if (this.riskCut) this.riskCut.textContent = fin > 0.35 ? ",
    ";\n      if (this.riskHeld) this.riskHeld.textContent = fin > 0.6 ? ",
    ";\n      if (this.riskNote) this.riskNote.style.opacity = String(this.cl((fin - 0.6) / 0.4));\n    }\n\n    if (this.covRowsUI) {\n      const rows = this.covRowsUI.length;\n      const fill = this.cl(pC / 0.72);\n      this.covRowsUI.forEach((r, i) => {\n        const k = this.ease(this.cl(fill * (rows + 4) - i));\n        r.style.opacity = String(k);\n        r.style.transform = ",
    ";\n      });\n      const fin = this.cl((pC - 0.74) / 0.24);\n      if (this.covTight) this.covTight.textContent = fin > 0.03 ? Math.round(10 * fin) + ",
    ";\n      if (this.covScore) this.covScore.textContent = fin > 0.7 ? ",
    ";\n      if (this.covNote) this.covNote.style.opacity = String(this.cl((fin - 0.6) / 0.4));\n    }\n\n    // ---- screen A -------------------------------------------------------\n    if (this.bizSegs) {\n      const fill = this.cl(pA / 0.55);\n      this.bizSegs.forEach((s, i) => {\n        const k = this.ease(this.cl(fill * this.bizSegs.length - i));\n        s.el.style.width = (s.pct * k) + ",
    ";\n      });\n      const bk = this.ease(this.cl((pA - 0.52) / 0.22));\n      if (this.bizBracket) this.bizBracket.style.width = (46 * bk) + ",
    ";\n      if (this.bizPct) this.bizPct.textContent = bk > 0.05 ? Math.round(46 * bk) + ",
    ";\n      const mk = this.cl((pA - 0.66) / 0.30);\n      if (this.bizMarks) this.bizMarks.forEach((m, i) => {\n        const k = this.ease(this.cl(mk * this.bizMarks.length - i));\n        m.style.transform = ",
    " + k.toFixed(3) + ",
    ";\n      });\n      if (this.bizWA) this.bizWA.textContent = (3.8 * this.cl((pA - 0.7) / 0.28)).toFixed(1) + ",
    ";\n    }\n\n    // ---- screen B -------------------------------------------------------\n    const m = this.p2MemoBridge;\n    if (!m) return;\n    const px = (v) => (v / m.MAX) * m.H;\n    const s1 = this.cl(br / 0.28), s2 = this.cl((br - 0.28) / 0.24), s3 = this.cl((br - 0.52) / 0.16);\n    const s4 = this.cl((br - 0.68) / 0.14), s5 = this.cl((br - 0.82) / 0.18);\n    m.R[0].el.style.height = px(12.8 * s1).toFixed(1) + ",
    ";\n    if (this.strike) {\n      const seg = m.X[1].el.getBoundingClientRect(), host = m.chart.getBoundingClientRect();\n      if (seg.width && host.width) {\n        this.strike.style.opacity = String(s4 * (1 - s5));\n        this.strike.style.left = (seg.left - host.left - 4) + ",
    ";\n      }\n    }\n    const eb = 14.6 + (13.9 - 14.6) * s5, lv = 4.19 + (4.39 - 4.19) * s5;\n    if (this.memoEbitda) this.memoEbitda.textContent = eb.toFixed(1);\n    if (this.memoLev) this.memoLev.textContent = lv.toFixed(2) + ",
    ";\n    if (this.memoHead) {\n      this.memoHead.textContent = (4.75 - lv).toFixed(2) + ",
    ";\n    }\n    if (this.srcRows) {\n      let lit = 0;\n      this.srcRows.forEach((r, i) => {\n        const k = this.ease(this.cl(ct * 4 - i));\n        if (k > 0.6) lit++;\n        r.line.style.width = (k * 22).toFixed(1) + ",
    ";\n        r.item.style.opacity = String(0.34 + 0.66 * k);\n      });\n      if (this.citeCount) this.citeCount.textContent = lit + ",
    ";\n    }\n  }\n\n  // ---- 4 committee: the sitting, the authority, the record ---------------\n  p2PaneIC(pane) {\n    const left = this.el(",
    "Agenda · 12 Mar, 14:00",
    "approved, conditions",
    "deferred, diligence",
    "));\n      const t = this.el(",
    "Quorum and conflicts",
    "Quorum",
    "5 of 5, met",
    "Recused",
    "R. Ashworth, Tarn",
    "circulated 12 Mar 18:40",
    "padding:10px 12px; background:rgba(21,127,82,0.07); border:1px solid rgba(21,127,82,0.24); border-radius:5px",
    "Northgate Cold Chain · decision",
    "hold 62.5 · within authority · referred on size",
    "Votes and dissent",
    ");\n      const w = this.el(",
    " + this.p2Tone(v[2]), v[2]));\n      row.appendChild(t);\n      if (v[2] === ",
    ") row.appendChild(this.el(",
    "Hedging covenant to 60 days, capex basket to 6.0. Recorded and attached as conditions 15 and 16.",
    "Conditions precedent",
    "Hedging within 60 days",
    "Capex basket at 6.0",
    "Conditions 15 and 16 created from committee dissent. Funding blocked until all satisfied.",
    "];\n    const left = this.el(",
    ");\n    const tpl = ",
    ";\n    const hd = this.el(",
    " + tpl + ",
    "Rating",
    "Leverage",
    "Migration",
    "Cert",
    "text-align:right",
    "on time",
    ");\n      const mg = this.el(",
    " + r[2]);\n      const st = this.el(",
    ");\n      const ct = this.el(",
    "flex:0 0 288px; min-width:0; padding-left:18px; border-left:1px solid rgba(20,20,18,0.08)",
    "Amendments and waivers",
    "liquidity waiver",
    "at committee",
    "capex basket",
    "priced, 25bps",
    "reporting cure",
    ");\n      const t = this.el(",
    ", r[1]));\n      right.appendChild(row);\n      return row;\n    });\n\n    right.appendChild(this.el(",
    "Concentration v LPA limits",
    "Largest borrower",
    "]].forEach((c) => {\n      const row = this.el(",
    ", c[1].toFixed(1) + c[3] + ",
    " + c[2] + c[3]));\n      row.appendChild(t);\n      const tr = this.el(",
    " + ((c[1] / c[2]) * 100).toFixed(0) + ",
    "));\n      row.appendChild(tr);\n      right.appendChild(row);\n    });\n    pane.appendChild(right);\n\n    this.p2WatchDim = [left, right];\n    this.p2BuildDossier(pane);\n    this.p2SetCerts(0);\n    this.p2SetDossier(0);\n  }\n\n  // the deal we followed, twelve months after close\n  p2BuildDossier(pane) {\n    const LO = 3.0, HI = 5.0, H = 110, COV = 4.75;\n    const p = this.el(",
    "Funded 12 Mar · unitranche 62.5 · four certificates in",
    "B+ held",
    "16 of 16 tests passed",
    "Leverage against covenant, quarter by quarter",
    "px; height:0; border-top:1px dashed rgba(196,52,30,0.55)",
    ");\n      const bar = this.el(",
    ");\n      c.appendChild(val); c.appendChild(bar);\n      plot.appendChild(c);\n      return { bar: bar, val: val, h: ((q[1] - LO) / (HI - LO)) * H, q: q };\n    });\n    chart.appendChild(plot);\n    const axis = this.el(",
    ");\n    qs.forEach((o) => {\n      const c = this.el(",
    ", o.q[0]));\n      const m = this.el(this.mono + ",
    "Scale from 3.00x · each certificate tested on receipt · no waiver requested",
    "flex:0 0 250px; min-width:0; padding-left:20px; border-left:1px solid rgba(20,20,18,0.08)",
    "Committee conditions",
    "15 · Hedging within 60 days",
    "satisfied 06 May",
    "16 · Capex basket at 6.0",
    "documented at close",
    ", c[1]));\n      side.appendChild(row);\n      notes.push(row);\n    });\n    const sum = this.el(",
    "4 of 4, on time",
    "B+ held since close",
    "Next test",
    "The card you opened on the pipeline board, twelve months on.",
    ";\n      r.rt.textContent = k > 0.62 ? d[2] : d[1];\n      r.mg.style.opacity = String(this.cl(k * 2.2 - 1.1));\n      r.mg.style.color = r.down ? ",
    ";\n      const done = k > 0.74;\n      r.st.textContent = k < 0.06 ? ",
    " : done ? d[5] : ",
    ";\n      r.st.style.color = done ? this.p2Tone(d[5]) : ",
    ";\n      r.ct.textContent = k > 0.28 ? d[6] : ",
    ";\n      r.ct.style.color = k > 0.28 && /late/.test(d[6]) ? ",
    ";\n      r.lv.style.color = done && d[5] !== ",
    " + (Math.sin(Math.PI * this.cl(k)) * 0.055).toFixed(3) + ",
    ";\n      if (done) { if (d[5] === ",
    ") pass++; else if (d[5] === ",
    ") watch++; else breach++; }\n    });\n    const recv = Math.min(24, Math.round(a * 24));\n    this.p2CertFoot.textContent = a < 0.02\n      ? ",
    "\n      : recv + ",
    " + Math.round((recv * 40) / 24) + ",
    " + pass + ",
    " + watch + ",
    " + breach + ",
    ";\n    if (this.p2AmendRows) this.p2AmendRows.forEach((r, i) => {\n      r.style.opacity = String(this.ease(this.cl((a - 0.60 - i * 0.10) / 0.16)));\n    });\n  }\n\n  p2SetDossier(o) {\n    const d = this.p2Dossier;\n    if (!d) return;\n    d.panel.style.opacity = String(o);\n    d.panel.style.transform = ",
    " + (0.974 + 0.026 * o).toFixed(4) + ",
    ";\n    if (this.p2WatchDim) this.p2WatchDim.forEach((n) => { n.style.opacity = String(1 - o * 0.8); });\n    d.qs.forEach((q, i) => {\n      const k = this.ease(this.cl(o * (d.qs.length + 2.2) - i * 1.15));\n      q.bar.style.height = (q.h * k).toFixed(1) + ",
    ";\n      q.val.style.opacity = String(this.cl(k * 1.8 - 0.6));\n      q.meta.style.opacity = String(this.cl(k * 1.8 - 0.85));\n    });\n    d.notes.forEach((nd, i) => { nd.style.opacity = String(this.ease(this.cl((o - 0.42 - i * 0.10) / 0.16))); });\n  }\n\n  // ---- 03 origination: a vintage year filtered gate by gate --------------\n  // 248 tiles enter; at each gate the ones that fail dim to an outline and their\n  // reason accumulates, while a vignette shows the product surface doing the work.\n  oBuild() {\n    const host = this.oField.current, cnt = this.oCount.current, vh = this.oVig.current;\n    if (!host || !cnt || !vh) return;\n    const N = 248, COLS = 16, PITCH = 30;\n    // sampled from the reference: paper creams, ice blues, sages, deep teals, one olive\n    const PAL = [",
    "));\n    const order = [];\n    for (let i = 0; i < N; i++) order.push(i);\n    order.sort((a, b) => this.h2(a, 11) - this.h2(b, 11));\n    const exitAt = new Array(N).fill(6);\n    let k = 0;\n    [[152, 1], [35, 2], [23, 3], [14, 4], [13, 5]].forEach((e) => {\n      for (let j = 0; j < e[0]; j++) exitAt[order[k++]] = e[1];\n    });\n    this.oTiles = [];\n    for (let i = 0; i < N; i++) {\n      const c = i % COLS, r = Math.floor(i / COLS);\n      const h1 = this.h2(c + 2, r + 5), h3 = this.h2(r + 11, c + 3);\n      // depth reads darker toward the middle of the field, as in the reference\n      const dc = Math.min(1, Math.hypot((c - 7.5) / 8.2, (r - 7.5) / 8.2));\n      const depth = this.cl((1 - dc) * 0.78 + (h1 - 0.5) * 0.66);\n      const col = h3 > 0.94 ? OLIVE[h1 > 0.5 ? 1 : 0] : PAL[Math.round(depth * (PAL.length - 1))];\n      const size = h3 < 0.16 ? 12 : h3 > 0.86 ? 21 : 16;\n      const x = c * PITCH + (PITCH - size) / 2, y = r * PITCH + (PITCH - size) / 2;\n      const t = this.el(",
    " + size + ",
    " + col + ",
    ");\n      host.appendChild(t);\n      // the sweep runs left to right with a slight diagonal, so a gate reads as a pass\n      const wave = this.cl((c / (COLS - 1)) * 0.86 + (r / (COLS - 1)) * 0.14);\n      this.oTiles.push({ el: t, exit: exitAt[i], x: x, y: y, size: size, col: col, wave: wave, last: ",
    ", this.oStages[0].name);\n    cnt.appendChild(this.oName);\n    const row = this.el(",
    ";\n    this.oVigs = this.oStages.map((s, i) => (i === 1 ? this.oGates(vh, s) : i === 2 ? this.oSheet(vh, s) : this.oCard(vh, s)));\n  }\n\n  // Screening is a rules engine, so it is shown as one: five policy gates on a rail,\n  // each measured against its own threshold, with the one that stops the deal stamped.\n  oGates(host, s) {\n    const card = this.el(",
    ", s.crumb));\n    const hrow = this.el(",
    "Halcyon Dental · 38.0",
    "; gap:12px; margin:16px 0 0; padding-bottom:6px; border-bottom:1px solid rgba(20,20,18,0.13); ",
    "Gate",
    "Policy",
    "Measured",
    "Verdict",
    ", t)));\n    card.appendChild(hd);\n\n    const GATES = [\n      [",
    "20.0 to 75.0 hold",
    "Sector policy",
    "not on excluded list",
    "dental roll-up",
    "at or under 4.75x",
    "UK, US, EU lending",
    "United Kingdom",
    "two prior deals closed",
    "var(--sec)",
    "; gap:12px; align-items:center; padding:11px 0; border-bottom:1px solid rgba(20,20,18,0.06); opacity:0",
    "));\n\n      const rail = this.el(",
    " + (i === GATES.length - 1 ? ",
    "));\n      const dot = this.el(",
    " + (fail ? ",
    " : tone(g[4])));\n      if (!fail) dot.style.background = tone(g[4]);\n      rail.appendChild(dot);\n      row.appendChild(rail);\n\n      const nm = this.el(",
    "GATE ",
    "color:",
    "background:#1A1917; color:#FCFBF9; border:1px solid #1A1917",
    "background:#F7F5F0; color:var(--sec); border:1px dashed rgba(20,20,18,0.34)",
    "background:#FFFFFF; color:var(--sec); border:1px solid rgba(20,20,18,0.2)",
    "flex:0 0 auto; padding:4px 9px; border:1px solid rgba(20,20,18,0.34); ",
    "Declined",
    "Stopped at gate 03 · policy 9.2 · 0.65x above the opening limit · reason recorded",
    "3 pass · 1 flag · 1 fail",
    "flex:0 0 72px; padding:6px 9px; border-right:1px solid rgba(20,20,18,0.13); ",
    "=D5/D3   ·   net debt over adjusted EBITDA, both tied to the audited file",
    ");\n    const sheet = this.el(",
    ");\n    const hcell = (t, i) => this.el(",
    "].forEach((t, i) => hd.appendChild(hcell(t, i)));\n    sheet.appendChild(hd);\n    const sub = this.el(",
    "1px solid rgba(20,20,18,0.13)",
    "; border-bottom:1px solid rgba(20,20,18,0.13); ",
    "Adjusted EBITDA",
    "EBITDA margin",
    "1px solid rgba(20,20,18,0.09)",
    " : src ? ",
    ") + (src ? ",
    ") +\n          (sel ? ",
    "), t);\n        row.appendChild(c);\n      });\n      sheet.appendChild(row);\n      return row;\n    });\n    card.appendChild(sheet);\n\n    const tabs = this.el(",
    "Adjustments",
    "Covenant test",
    "Cash flow",
    "padding:6px 13px; border-right:1px solid rgba(20,20,18,0.09); ",
    "), t));\n    });\n    card.appendChild(tabs);\n\n    const chip = this.el(",
    ", s.chip);\n    card.appendChild(chip);\n    host.appendChild(card);\n    return { card: card, rows: rows, chip: chip };\n  }\n\n  oCard(host, s) {\n    const card = this.el(",
    ", s.head));\n    const rows = s.rows.map((r, i) => {\n      const row = this.el(",
    "), r[1]));\n      card.appendChild(row);\n      return row;\n    });\n    const chip = this.el(",
    ";\n    card.appendChild(chip);\n    host.appendChild(card);\n    return { card: card, rows: rows, chip: chip };\n  }\n\n  // every vignette takes the height of the tallest, so the window does not jump\n  // between beats; re-measured when the type or the stage width changes\n  oEqualize() {\n    if (!this.oVigs || !this.oWrap.current) return;\n    const w = Math.round(this.oWrap.current.getBoundingClientRect().width);\n    this._oEqN = (this._oEqN || 0) + 1;\n    if (this._oEqW === w && this._oEqN > 5) return;\n    this._oEqW = w;\n    this.oVigs.forEach((v) => { v.card.style.height = ",
    "; });\n    let h = 0;\n    this.oVigs.forEach((v) => { h = Math.max(h, v.card.offsetHeight); });\n    if (h > 0) this.oVigs.forEach((v) => { v.card.style.height = h + ",
    "; });\n  }\n\n  oSync() {\n    const track = this.oTrack.current, pin = this.oPin.current, wrap = this.oWrap.current, stage = this.oStage.current;\n    if (!track || !pin) return;\n    this.pinFit(this.oFit, this.oWrap, 1240, 700);\n    if (!this.oTiles) { this.oBuild(); if (!this.oTiles) return; }\n    if (wrap && stage) {\n      const r = wrap.getBoundingClientRect();\n      if (r.width) {\n        // scale on both axes: the declines band is content, not backdrop, so it\n        // must never fall outside the clip on a short viewport\n        const k = Math.min(1, r.width / 1200, (r.height || 720) / 720);\n        const tx = Math.max(0, (r.width - 1200 * k) / 2);\n        const key = k.toFixed(4) + ",
    " + tx.toFixed(1);\n        if (stage.dataset.k !== key) {\n          stage.dataset.k = key;\n          stage.style.transform = ",
    " + tx.toFixed(1) + ",
    ";\n        }\n      }\n    }\n    this.oEqualize();\n    const span = track.offsetHeight - pin.offsetHeight;\n    const p = span > 0 ? this.cl(-track.getBoundingClientRect().top / span) : 0;\n    const n = this.oStages.length;\n    const prog = this.seg(p, 0.05, 0.97) * n;\n    const idx = Math.min(n - 1, Math.floor(prog));\n    const local = this.cl(prog - idx);\n\n    // Each gate is a sweep across the field: as the wave reaches a tile the ones\n    // that pass pop and sharpen, and the ones that fail defocus and sink into the\n    // paper. Styles are written only when they change, so 248 tiles stay cheap.\n    // the eleven that hold turn live where they stand\n    const g = idx === n - 1 ? this.ease(this.cl((local - 0.42) / 0.46)) : 0;\n    this.oTiles.forEach((t) => {\n      const hit = t.exit + 0.06 + t.wave * 0.30;\n      const bell = Math.max(0, 1 - Math.abs(prog - (idx + 0.06 + t.wave * 0.30)) / 0.085);\n      if (t.exit === n) {\n        const gk = this.ease(this.cl(g * 14 - t.si * 0.85));\n        const s = 1 + 0.16 * bell * (1 - g) + 0.24 * gk;\n        const css = ",
    " + s.toFixed(3) + ",
    ";\n        const bg = gk > 0.06 ? this.oLive : t.col;\n        const fil = gk > 0.04 ? ",
    " : bell > 0.02 ? ",
    " + (1 + 0.5 * bell).toFixed(2) + ",
    ";\n        const key = css + bg + fil;\n        if (t.last !== key) {\n          t.last = key;\n          t.el.style.transform = css;\n          t.el.style.background = bg;\n          t.el.style.filter = fil;\n          t.el.style.boxShadow = gk > 0.4 ? ",
    " + (10 * gk).toFixed(1) + ",
    " + (0.42 * gk).toFixed(2) + ",
    ";\n        }\n        return;\n      }\n      const d = this.ease(this.cl((prog - hit) / 0.24));\n      const key = d.toFixed(3) + (bell > 0.02 ? bell.toFixed(2) : ",
    ");\n      if (t.last === key) return;\n      t.last = key;\n      if (d >= 1) {\n        t.el.style.opacity = ",
    " + (7 * d).toFixed(2) + ",
    " + (1 + 0.5 * bell - 0.7 * d).toFixed(2) + ",
    " + (1 + 0.16 * bell - 0.3 * d).toFixed(3) + ",
    ";\n    });\n\n    const from = idx === 0 ? this.oStages[0].n : this.oStages[idx - 1].n;\n    const tick = idx === 0 ? 1 : this.ease(this.cl((local - 0.05) / 0.4));\n    const cur = Math.round(from + (this.oStages[idx].n - from) * tick);\n    if (this.oBig) this.oBig.textContent = String(cur);\n    if (this.oName && this.oName.textContent !== this.oStages[idx].name) this.oName.textContent = this.oStages[idx].name;\n    if (this.oLabel && this.oLabel.textContent !== this.oStages[idx].label) this.oLabel.textContent = this.oStages[idx].label;\n    if (this.oTicks) this.oTicks.forEach((t, i) => {\n      t.style.background = i === idx ? ",
    " : i < idx ? ",
    ";\n    });\n\n    if (this.oVigs) this.oVigs.forEach((v, i) => {\n      const inK = this.ease(this.cl((prog - i) / 0.14));\n      const outK = i === n - 1 ? 0 : this.cl((prog - i - 0.92) / 0.08);\n      const o = inK * (1 - outK);\n      v.card.style.opacity = String(o);\n      v.card.style.transform = ",
    " + ((1 - inK) * 16).toFixed(1) + ",
    ";\n      if (o > 0.02) {\n        const rr = this.ease(this.cl((prog - i - 0.10) / 0.45));\n        v.rows.forEach((row, j) => { row.style.opacity = String(this.ease(this.cl(rr * (v.rows.length + 2) - j))); });\n        v.chip.style.opacity = String(this.ease(this.cl((prog - i - 0.58) / 0.2)));\n      }\n    });\n  }\n\n  // ---- 04 servicing -------------------------------------------------------\n  buildServ() {\n    const h = this.servHost.current;\n    if (!h) return;\n    h.textContent = ",
    ";\n    const num = (v, d) => v.toLocaleString(",
    ", { minimumFractionDigits: d, maximumFractionDigits: d });\n    const SH = ",
    ";\n    const chip = (pos, proc, done) => {\n      const el = this.el(",
    " + pos);\n      const icon = this.el(",
    ");\n      const dots = this.el(",
    ");\n      const d = [0, 1, 2].map(() => {\n        const x = this.el(",
    ");\n        dots.appendChild(x);\n        return x;\n      });\n      const check = this.el(",
    " + this.accent + ",
    ";\n          label.textContent = ph === 2 ? done : proc;\n          check.style.opacity = ph === 2 ? ",
    ";\n          dots.style.opacity = ph === 2 ? ",
    ";\n          if (ph === 1) d.forEach((x, i) => { x.style.opacity = (0.25 + 0.75 * (0.5 + 0.5 * Math.sin(t * 6 - i * 0.9))).toFixed(2); });\n        }\n      };\n    };\n    const cell = (title) => {\n      const c = this.el(",
    ");\n      const stage = this.el(",
    ", title));\n      h.appendChild(c);\n      return stage;\n    };\n    const kv = (host, rows, valStyle) => rows.map((r, i) => {\n      const row = this.el(",
    ") + (valStyle || ",
    "), r[1]);\n      row.appendChild(v);\n      host.appendChild(row);\n      return v;\n    });\n\n    // --- 1. the rate build recomputes itself on every reset -----------------\n    const s1 = cell(",
    ")));\n    const sf1 = this.el(",
    "Ardyne Packaging · 01\\u201330 Jun",
    "Base, SOFR 30d",
    "Margin, grid level 2",
    "Floor, not binding",
    "All-in rate",
    "Interest accrued, 30/360",
    "Recomputing rate",
    "Reset applied · 5.41%",
    "Payments applied down the waterfall.",
    "Wire received · 04 Jul",
    ");\n    h2.appendChild(tot);\n    sf2.appendChild(h2);\n    const g2 = this.el(",
    ");\n    sf2.appendChild(g2);\n    const ALLOC = [[",
    ", 126000, 22]];\n    const bars = ALLOC.map((a, i) => {\n      const row = this.el(",
    ");\n      const top = this.el(",
    ", a[0]));\n      const v = this.el(this.mono + ",
    ");\n      top.appendChild(v);\n      row.appendChild(top);\n      const track = this.el(",
    ");\n      const fill = this.el(",
    ");\n      track.appendChild(fill);\n      row.appendChild(track);\n      g2.appendChild(row);\n      return { v: v, fill: fill, to: a[1], w: a[2] };\n    });\n    sf2.appendChild(this.el(",
    "Position, fund ledger and investor report, one entry",
    "Applying waterfall",
    "Applied · 3 ledgers",
    "Notices drafted, sent and logged.",
    "Clause 8.2 · interest payment notice",
    "Notice of interest due",
    "Interest period",
    "Amount due",
    "Payment date",
    "margin:12px 0 0; padding-top:10px; border-top:1px solid rgba(20,20,18,0.06)",
    "Drafted from the agreement",
    "Figures tied to the ledger",
    "Sent to borrower and agent",
    "Drafting notice",
    "Sent · 3 parties",
    "translateY(",
    ";\n        ld1.style.opacity = vis.toFixed(2);\n        const v = this.ease(this.cl((k - 0.2) / 0.6));\n        r1[0].textContent = (5.33 + 0.08 * v).toFixed(2) + ",
    ";\n        r1[0].style.color = v > 0.05 ? ",
    ";\n        r1[4].textContent = num(0.607 * v, 3);\n      } },\n      { chip: c2, set: (k, vis, inK) => {\n        rise(sf2, inK, vis, 18);\n        const v = this.ease(this.cl(k / 0.82));\n        tot.textContent = num(1842500 * v, 0);\n        bars.forEach((b, i) => {\n          const bk = this.ease(this.cl(v * 3 - i * 0.55));\n          b.fill.style.width = (b.w * bk).toFixed(1) + ",
    ";\n          b.v.textContent = num(b.to * bk, 0);\n        });\n      } },\n      { chip: c3, set: (k, vis, inK) => {\n        rise(sf3, inK, vis, 20);\n        const v = this.cl(k / 0.86);\n        ticks.forEach((t, i) => { t.style.opacity = (vis * this.ease(this.cl(v * 3.4 - i * 0.9))).toFixed(3); });\n      } }\n    ];\n    this.servA = this.servCards;\n    this.loopServ();\n  }\n\n  // Each card runs the same three-state loop on its own offset: the surface\n  // arrives, the work is shown processing, then it settles as done and repeats.\n  loopServ() {\n    if (!this.servCards) return;\n    const t = this._t || 0, CY = 6.2;\n    this.servCards.forEach((c, i) => {\n      const u = ((t / CY) + i * 0.11) % 1;\n      const inK = this.ease(this.cl(u / 0.07));\n      const vis = inK * (1 - this.ease(this.cl((u - 0.90) / 0.10)));\n      c.set(this.cl((u - 0.12) / 0.40), vis, inK);\n      c.chip.show(u < 0.17 ? 0 : u < 0.56 ? 1 : u < 0.88 ? 2 : 3, t);\n    });\n  }\n\n  stepServ() {\n    if (this.servCards) this.loopServ();\n  }\n\n  // ---- 05 investors: the allocation engine --------------------------------\n  // One deal at a time is tested against every mandate we manage, then split\n  // across the vehicles that can legally hold it. Runs on its own clock.\n  buildLp() {\n    const h = this.lpHost.current;\n    if (!h) return;\n    h.textContent = ",
    ";\n    const VH = 116, VG = 14, TOP = 34;\n    this.lpDeals = [\n      { b: ",
    "]] }\n    ];\n    const VEH = [\n      [",
    "Flagship fund · $1.2bn",
    "Direct lending · min B− · max 12% single",
    "Managed account · $300m",
    "Senior secured only · min B · max 8%",
    "Sleeve · $450m",
    "Junior and opportunistic · min CCC+",
    "Rated feeder · $250m",
    "No PIK · min B+ · max 6% single",
    "]\n    ];\n\n    const win = this.el(",
    ");\n    const bar = this.el(",
    ");\n    const lights = this.el(",
    "Allocation",
    "padding:26px clamp(16px,1.8vw,24px); border-right:1px solid rgba(20,20,18,0.08); min-width:0",
    "Ready to allocate",
    " + d.size.toFixed(1) + ",
    "));\n      row.appendChild(sub);\n      qwrap.appendChild(row);\n      return { row: row, mark: mark, nm: nm, sub: sub };\n    });\n    q.appendChild(this.el(",
    "Mandates read from the LPA, the side letters and each managed account agreement",
    "Deal on the desk",
    ");\n    mid.appendChild(this.lpName);\n    const chips = this.el(",
    ");\n    mid.appendChild(chips);\n    this.lpChips = [0, 1, 2].map(() => {\n      const c = this.el(",
    ");\n      chips.appendChild(c);\n      return c;\n    });\n    const sizeRow = this.el(",
    "to place",
    "margin:24px 0 0; padding-top:16px; border-top:1px solid rgba(20,20,18,0.08); ",
    ");\n    mid.appendChild(this.lpSplit);\n    this.lpSplitParts = [0, 1, 2, 3].map((i) => {\n      const p = this.el(",
    " + (0.62 - i * 0.13).toFixed(2) + ",
    ");\n      this.lpSplit.appendChild(p);\n      return p;\n    });\n    this.lpFoot = this.el(",
    ");\n    mid.appendChild(this.lpOrigin);\n    body.appendChild(mid);\n\n    // connector gutter: a grey routing line with money moving along it\n    const gut = this.el(",
    ");\n    gut.appendChild(this.lpStub);\n    this.lpFlow = [0, 1, 2, 3].map(() => {\n      const dot = this.el(",
    ");\n      gut.appendChild(dot);\n      return dot;\n    });\n    body.appendChild(gut);\n\n    // vehicles\n    const right = this.el(",
    "Mandates under management",
    ", v[2]));\n      const tests = this.el(",
    ");\n      const chipsT = [",
    ", t));\n        tests.appendChild(c);\n        return { c: c, g: g };\n      });\n      col.appendChild(tests);\n      card.appendChild(col);\n      vwrap.appendChild(card);\n      return { card: card, branch: branch, dot: dot, amt: amt, tests: chipsT, why: why, y: TOP + i * (VH + VG) + VH / 2 };\n    });\n    body.appendChild(right);\n    h.appendChild(win);\n    this.lpRows = this.lpVeh;\n    this.loopLp();\n  }\n\n  loopLp() {\n    if (!this.lpVeh) return;\n    const t = this._t || 0, CY = 9.4;\n    const n = this.lpDeals.length;\n    const raw = (t / CY) % n;\n    const idx = Math.floor(raw);\n    const u = raw - idx;\n    const d = this.lpDeals[idx];\n    const inK = this.ease(this.cl(u / 0.07));\n    const out = 1 - this.ease(this.cl((u - 0.93) / 0.07));\n    const test = this.cl((u - 0.12) / 0.26);\n    const al = this.ease(this.cl((u - 0.42) / 0.3));\n    const settled = u > 0.74 && u < 0.93;\n\n    this.lpQueue.forEach((q, i) => {\n      // the queue is content, not chrome: dim the detail line, never the borrower\n      q.mark.style.opacity = i === idx ? String(inK * out) : ",
    ";\n      q.nm.style.color = i === idx ? ",
    ";\n      q.sub.style.color = i === idx ? ",
    ";\n    });\n    this.lpName.textContent = d.b;\n    this.lpName.style.opacity = (inK * out).toFixed(2);\n    [d.f, d.sec, d.r].forEach((tx, i) => {\n      this.lpChips[i].textContent = tx;\n      this.lpChips[i].style.opacity = (inK * out).toFixed(2);\n    });\n    const placed = d.size * al;\n    this.lpSize.textContent = ",
    " + (d.size - placed).toFixed(1) + ",
    ";\n    this.lpState.textContent = u < 0.12 ? ",
    "\n      : test < 1 ? ",
    " + Math.min(4, Math.ceil(test * 4)) + ",
    ";\n    this.lpState.style.color = al >= 1 ? ",
    ";\n    this.lpOrigin.style.opacity = (this.ease(this.cl((u - 0.38) / 0.08)) * out).toFixed(2);\n\n    const total = d.size;\n    const shares = [0, 0, 0, 0];\n    d.alloc.forEach((a) => { shares[a[0]] = a[1] / total; });\n    this.lpSplitParts.forEach((p, i) => { p.style.width = (shares[i] * 100 * al).toFixed(1) + ",
    "; });\n    this.lpFoot.textContent = settled\n      ? ",
    " + total.toFixed(1) + ",
    " + d.alloc.length + ",
    ";\n\n    const eligible = d.alloc.map((a) => a[0]);\n    // endpoints measured live, so the spine always meets the branches it feeds\n    const gr = this.lpSpine.parentElement.getBoundingClientRect();\n    const sx = gr.width / 2;\n    // the run from the spine to each card crosses the gutter's right half plus the\n    // column's own padding, which both move with the viewport: measure, don't assume\n    const spans = this.lpVeh.map((v) => Math.max(8, Math.round(v.card.getBoundingClientRect().left - (gr.left + sx + 1))));\n    const spineTop = Math.round(this.lpOrigin.getBoundingClientRect().top - gr.top) + 3;\n    const ys = eligible.map((i) => Math.round(this.lpVeh[i].branch.getBoundingClientRect().top - gr.top) + 1);\n    const lo = Math.min(spineTop, Math.min.apply(null, ys)), hi = Math.max(spineTop, Math.max.apply(null, ys));\n    const draw = this.ease(this.cl((u - 0.40) / 0.16)) * out;\n    this.lpSpine.style.top = lo + ",
    ";\n    this.lpStub.style.opacity = draw.toFixed(2);\n\n    // money travels the route: out of the deal, down the spine, into each mandate\n    const flowing = u > 0.44 && u < 0.82 ? 1 : 0;\n    this.lpFlow.forEach((dot, ei) => {\n      const vi = eligible[ei];\n      if (vi === undefined || !flowing) { dot.style.opacity = ",
    "; return; }\n      const share = d.alloc[ei][1] / total;\n      const ph = ((t * 0.62) + ei * 0.3) % 1;\n      const s1 = this.cl(ph / 0.15);\n      const s2 = this.ease(this.cl((ph - 0.15) / 0.45));\n      const s3 = this.cl((ph - 0.6) / 0.4);\n      const x = sx * s1 + spans[vi] * s3;\n      const y = spineTop + (ys[ei] - spineTop) * s2;\n      const sz = 6 + 5 * share;\n      dot.style.width = sz.toFixed(1) + ",
    " + (-sz / 2).toFixed(1) + ",
    " + x.toFixed(1) + ",
    " + y.toFixed(1) + ",
    " + (3 * (1 - s3)).toFixed(1) + ",
    ";\n      const land = this.lpVeh[vi];\n      land.dot.style.boxShadow = s3 > 0.9 ? ",
    ";\n    });\n\n    this.lpVeh.forEach((v, i) => {\n      // cards grow when a mandate line wraps, so the connector point follows the card\n      const cy = Math.round(v.card.offsetHeight / 2);\n      const span = spans[i];\n      if (v.cy !== cy || v.span !== span) {\n        v.cy = cy;\n        v.span = span;\n        v.branch.style.top = cy + ",
    ";\n      }\n      const fail = d.fails.find((f) => f[0] === i);\n      const ei = eligible.indexOf(i);\n      const ok = ei >= 0;\n      const resolved = this.cl(test * 4 - i);\n      v.tests.forEach((c, j) => {\n        const on = resolved > (j + 1) / 3.4;\n        const bad = !!fail && ((j === 0 && /class|secured|hurdle/.test(fail[1])) || (j === 1 && /rating/.test(fail[1])));\n        c.c.style.opacity = on ? ",
    ";\n        c.g.textContent = on ? (bad ? ",
    ";\n        c.g.style.background = on ? (bad ? ",
    ";\n        c.c.style.borderColor = on && bad ? ",
    ";\n      });\n      const dim = fail && resolved > 0.7;\n      // ineligible reads structurally, in beige, so its reason stays legible\n      v.card.style.opacity = out.toFixed(2);\n      v.card.style.background = dim ? ",
    ";\n      v.card.style.borderColor = ok && al > 0.3 ? ",
    ";\n      v.why.textContent = fail ? fail[1] : ",
    ";\n      v.why.style.opacity = dim ? out.toFixed(2) : ",
    ";\n      const bk = ok ? this.ease(this.cl((u - 0.44 - ei * 0.05) / 0.12)) * out : 0;\n      v.branch.style.width = (spans[i] * bk).toFixed(1) + ",
    ";\n      v.dot.style.opacity = bk.toFixed(2);\n      if (ok) {\n        const amt = d.alloc[ei][1] * al;\n        v.amt.textContent = ",
    " + amt.toFixed(1) + ",
    ";\n        v.amt.style.opacity = (this.ease(this.cl((al - 0.05) / 0.2)) * out).toFixed(2);\n      } else {\n        v.amt.style.opacity = ",
    ";\n      }\n    });\n  }\n\n  stepLp() {\n    if (this.lpVeh) this.loopLp();\n  }\n\n  // ---- 06 delivery: an exploded stack, one layer in focus per beat ------\n  dIcon(i) {\n    const w = this.el(",
    ");\n    const ink = ",
    ";\n    const line = (s) => w.appendChild(this.el(",
    " + s));\n    const ring = (s) => w.appendChild(this.el(",
    " + s));\n    if (i === 0) {\n      ring(",
    ");\n    } else if (i === 1) {\n      ring(",
    ");\n    } else if (i === 2) {\n      ring(",
    ");\n    } else {\n      ring(",
    ");\n    }\n    return w;\n  }\n\n  // left, right and front corners of the isometric module, in stage coordinates\n  get dRP() { return [[-90, -8], [90, -8], [0, 32]]; }\n\n\n  // One layer of the stack, built as an actual box so the side walls read as walls:\n  // dark plate on top, two extruded faces toward the viewer, vents cut into them.\n  dPuck(host, i) {\n    const W = 128, D = 18, R = 17, WW = W - 34;\n    const y = 112;\n    // the floor glow is three offset pools rather than one ellipse, jittered per layer,\n    // so it reads as light on a surface instead of a drawn shape\n    const j = (a, b) => this.h2(i * 7 + a, b);\n    const gw = Math.round(196 + j(1, 2) * 44), gh = Math.round(26 + j(2, 5) * 16);\n    const pool = (s, x, cy, col, a, stop) => ",
    " + (56 + j(3, x) * 28).toFixed(0) + ",
    " + stop + ",
    ";\n    const glow = this.el(",
    " + Math.round(4 + j(4, 1) * 7) + ",
    " +\n      pool(52, Math.round(38 + j(5, 2) * 16), 52, ",
    " +\n      pool(40, Math.round(64 + j(6, 3) * 14), Math.round(40 + j(7, 4) * 20), ",
    " +\n      pool(34, Math.round(20 + j(8, 5) * 12), Math.round(50 + j(9, 6) * 22), ",
    " + (8 + j(10, 7) * 5).toFixed(1) + ",
    ");\n    host.appendChild(glow);\n\n    const w = this.el(",
    ");\n    const box = this.el(",
    ");\n\n    const under = this.el(",
    ");\n    box.appendChild(under);\n\n    const walls = [",
    "].map((kind) => {\n      const el = this.el(",
    " +\n        (kind === ",
    "));\n      const grad = this.el(",
    " + this.dGrad + ",
    " + (kind === ",
    ");\n      el.appendChild(grad);\n      const lip = this.el(",
    ");\n      el.appendChild(lip);\n      const vent = this.el(",
    ");\n      for (let k = 0; k < (kind === ",
    "));\n      el.appendChild(vent);\n      box.appendChild(el);\n      return { el: el, grad: grad, lip: lip };\n    });\n\n    const top = this.el(",
    "));\n    const sheen = this.el(",
    ");\n    top.appendChild(sheen);\n    const icon = this.dIcon(i);\n    top.appendChild(icon);\n    box.appendChild(top);\n\n    const rimBase = this.el(",
    ");\n    box.appendChild(rimBase);\n    const rim = this.el(",
    ");\n    box.appendChild(rim);\n\n    w.appendChild(box);\n    host.appendChild(w);\n\n    const pins = this.dRP.map((p) => {\n      const pn = this.el(",
    ");\n      host.appendChild(pn);\n      return pn;\n    });\n    return { w: w, top: top, icon: icon, sheen: sheen, rim: rim, walls: walls, glow: glow, pins: pins, y: y };\n  }\n\n  dBuild() {\n    const host = this.dStack.current;\n    if (!host) return;\n    host.textContent = ",
    ");\n    host.appendChild(this.dBloom);\n    this.dRails = this.dRP.map((p) => {\n      const r = this.el(",
    ");\n      host.appendChild(r);\n      return r;\n    });\n    this.dMods = [0, 1, 2, 3].map((i) => this.dPuck(host, i));\n    this.dLayout();\n    this.dSync();\n  }\n\n  // module pitch is derived from the column the stack actually got, so a shorter\n  // viewport shrinks the stack instead of scaling the whole section down\n  dLayout() {\n    const host = this.dStack.current;\n    if (!host || !this.dMods) return;\n    const H = host.clientHeight;\n    if (!H || this._dH === H) return;\n    this._dH = H;\n    const half = 50, lift = 12;\n    // the first plate sits just inside the column; the rest share what is left, so a\n    // taller column reads as wider gaps rather than a bigger top margin\n    const S = half + lift + 8;\n    const P = Math.max(30, (H - S - half - lift) / 3);\n    const RP = this.dRP;\n    this.dMods.forEach((m, i) => {\n      m.y = Math.round(S + i * P);\n      m.w.style.top = m.y + ",
    "; });\n    });\n    if (this.dRails) this.dRails.forEach((r, j) => {\n      r.style.top = Math.round(S + RP[j][1]) + ",
    ";\n    });\n  }\n\n  dSync() {\n    const track = this.dTrack.current, pin = this.dPin.current;\n    if (!track || !pin) return;\n    if (!this.dMods) { this.dBuild(); if (!this.dMods) return; }\n    this.dLayout();\n    const inner = this.dInner.current;\n    if (inner) {\n      // the layout is fluid and viewport-derived, so it normally sits at 1x and\n      // fills the page width; the floor stops it ever reading smaller than its peers\n      const cs = getComputedStyle(pin);\n      const avail = pin.clientHeight - parseFloat(cs.paddingTop) - parseFloat(cs.paddingBottom);\n      const need = inner.offsetHeight;\n      const k = need > 0 ? Math.max(0.94, Math.min(1, avail / need)) : 1;\n      const key = k.toFixed(3);\n      if (inner.dataset.k !== key) {\n        inner.dataset.k = key;\n        inner.style.transform = k < 0.999 ? ",
    " + key + ",
    ";\n      }\n    }\n    const span = track.offsetHeight - pin.offsetHeight;\n    const p = span > 0 ? this.cl(-track.getBoundingClientRect().top / span) : 0;\n    const n = 5;\n    const prog = this.seg(p, 0.05, 0.97) * n;\n    const beat = Math.min(n - 1, Math.floor(prog));\n    const local = this.cl(prog - beat);\n    const enter = this.ease(this.cl(local / 0.24));\n    // beat five closes the four layers into one unit and brings the CTA alongside\n    const asm = beat === n - 1 ? this.ease(this.cl(local / 0.58)) : 0;\n    const act = Math.min(3, beat);\n    const P = this.dMods.length > 1 ? this.dMods[1].y - this.dMods[0].y : 0;\n    const yMid = this.dMods[0].y + P * 1.5;\n\n    let focusY = 0, focusOn = 0;\n    this.dMods.forEach((m, i) => {\n      const hero = asm > 0 ? (i === 0 ? asm : 0) : 0;\n      const on = Math.max(asm > 0 ? 0 : (i === act ? enter : 0), hero);\n      const done = Math.max(i < act ? 1 : i === act ? enter : 0, asm);\n      const closed = (yMid + (i - 1.5) * 15) - m.y;\n      const shift = -12 * on * (1 - asm) + closed * asm;\n      m.w.style.transform = ",
    " + shift.toFixed(1) + ",
    ";\n      m.walls.forEach((wl) => {\n        wl.grad.style.opacity = Math.max(on, done * 0.22).toFixed(3);\n        wl.lip.style.background = ",
    " + (0.14 + 0.5 * on).toFixed(2) + ",
    ";\n      });\n      m.rim.style.opacity = (0.1 * done + 0.9 * on).toFixed(3);\n      m.w.style.opacity = (0.74 + 0.1 * done + 0.16 * on).toFixed(3);\n      m.sheen.style.opacity = on.toFixed(3);\n      m.glow.style.opacity = (asm > 0 ? (i === 3 ? 0.55 * asm : 0) : 0.22 * done + 0.7 * on).toFixed(3);\n      m.top.style.boxShadow = on > 0.05 ? ",
    " + (0.07 * on).toFixed(3) + ",
    ";\n      m.icon.style.opacity = (0.12 + 0.26 * done + 0.6 * on).toFixed(3);\n      m.icon.style.filter = on > 0.3 ? ",
    ";\n        pn.style.width = pn.style.height = on > 0.5 ? ",
    " + (0.26 + 0.24 * done + 0.4 * on).toFixed(2) + ",
    ";\n      });\n      if (on > focusOn) { focusOn = on; focusY = m.y + shift; }\n      void i;\n    });\n\n    if (this.dBloom) {\n      this.dBloom.style.top = focusY.toFixed(0) + ",
    ";\n      this.dBloom.style.opacity = (0.92 * focusOn * (1 - asm * 0.4)).toFixed(3);\n    }\n    if (this.dRails) this.dRails.forEach((r) => { r.style.opacity = (1 - asm).toFixed(2); });\n\n    // each cell reaches toward the stack with its own stub, so the leader needs no\n    // absolute coordinates and survives any column width\n    [this.dC1, this.dC2, this.dC3, this.dC4].forEach((ref, i) => {\n      const el = ref && ref.current;\n      if (!el) return;\n      // a stage that has run stays on the page: the list accumulates as you scroll\n      const arrive = i < act ? 1 : i === act ? enter : 0;\n      const on = i === act ? enter : 0;\n      el.style.opacity = ((i <= act ? Math.max(0.62, arrive) : 0) * (1 - asm)).toFixed(3);\n      el.style.transform = ",
    " + ((1 - arrive) * 9 - asm * 10).toFixed(1) + ",
    ";\n      const stub = el.lastElementChild;\n      if (stub && stub.hasAttribute(",
    ")) {\n        stub.style.width = (26 * arrive * (1 - asm)).toFixed(1) + ",
    ";\n        stub.style.opacity = ((0.4 + 0.6 * on) * (1 - asm)).toFixed(2);\n      }\n    });\n\n    const cta = this.dCta.current;\n    if (cta) {\n      cta.style.opacity = asm.toFixed(3);\n      cta.style.transform = ",
    " + ((1 - asm) * 16).toFixed(1) + ",
    ";\n      cta.style.pointerEvents = asm > 0.6 ? ",
    ";\n    }\n  }\n\n  frame() {\n    const motion = this.props.motion ?? true;\n    // PORT SHIM (3/4): frozenT pins the time-driven loops for deterministic QA captures\n    const t = this.frozenT != null ? this.frozenT : (performance.now() - this.t0) / 1000;\n    this._t = t;\n    if (motion && this.servCards && this.inView(this.servHost.current, 140)) this.step(",
    ");\n    if (motion && this.lpVeh && this.inView(this.lpHost.current, 140)) this.step(",
    ");\n    if (motion && this.inView(this.heroCv.current, 200)) {\n      try { this.drawHeroRing(t); } catch (e) {\n        this._errs = this._errs || {};\n        if (!this._errs.drawHeroRing) {\n          this._errs.drawHeroRing = 1;\n          window.__dcErrs = window.__dcErrs || [];\n          window.__dcErrs.push(",
    "oSync",
    "stepServ",
    "stepLp",
    "dSync",
    "p2Sync",
    " });\n  }\n\n  runIntro() {\n    const wrap = this.iWrap.current, st = this.iStage.current, mark = this.iMark.current;\n    if (!wrap || !st || !mark) return;\n    wrap.style.display = ",
    ";\n    const E = ",
    ";\n    const accent = this.props.accent ?? ",
    ";\n    const W = Math.min(620, window.innerWidth * 0.84);\n    const box = this.iEl(",
    ");\n    st.appendChild(box);\n\n    const head = this.iEl(",
    "flex:1",
    "Net asset value",
    "Management fee",
    "Carried interest",
    "FX translation",
    "Accrued expenses",
    "Variance",
    ";\n    const words = t.text.split(",
    ");\n    const spans = words.map((word, i) => {\n      const bare = word.replace(/[^A-Za-z0-9.]/g, ",
    ");\n      const hot = t.emph.indexOf(bare) >= 0;\n      const s = document.createElement(",
    ");\n      s.textContent = word + (i === words.length - 1 ? ",
    " +\n        (hot ? ",
    ") + (hot ? ",
    ");\n      el.appendChild(s);\n      return s;\n    });\n    if (ref === this.titleA) this.wordsA = spans; else this.wordsB = spans;\n  }\n\n  revealWords(spans, prog) {\n    if (!spans) return;\n    const n = spans.length;\n    spans.forEach((s, i) => {\n      const k = this.ease(this.cl(prog * (n + 3) - i));\n      s.style.opacity = String(k);\n      s.style.transform = ",
    " + ((1 - k) * 14).toFixed(2) + ",
    ";\n    });\n  }\n\n  win(ref, k, rise) {\n    const el = ref.current;\n    if (!el) return;\n    el.style.opacity = String(k);\n    el.style.transform = ",
    " + ((1 - k) * (rise || 22)).toFixed(2) + ",
    " +\n      (0.982 + 0.018 * k).toFixed(4) + ",
    ";\n  }\n\n  sync() {\n    const track = this.track.current, pin = this.pin.current, wrap = this.wrap.current, stage = this.stage.current;\n    if (!track || !pin) return;\n    this.pinFit(this.rFit, this.wrap, 1200, 780);\n    const rCrop = this.cropOf(this.wrap, 1200, 780);\n    [this.badgeA].forEach((r) => {\n      if (r.current && r.current.dataset.crop !== String(rCrop)) {\n        r.current.dataset.crop = String(rCrop);\n        r.current.style.bottom = (36 + rCrop) + ",
    ";\n      }\n    });\n\n    if (wrap && stage) {\n      const w = wrap.getBoundingClientRect().width;\n      if (w) {\n        const k = Math.min(1, w / 1200).toFixed(4);\n        if (stage.dataset.k !== k) {\n          stage.dataset.k = k;\n          stage.style.transform = ",
    ";\n        }\n      }\n    }\n\n    // A template patch re-creates the h2 and orphans the word spans built at mount.\n    // Rebuild whenever the element comes back empty so an edit cannot blank the titles.\n    if (this.titleA.current && this.titleA.current.children.length === 0) this.buildTitle(this.titleA, this.tA);\n    if (this.titleB.current && this.titleB.current.children.length === 0) this.buildTitle(this.titleB, this.tB);\n    if (this.pfSide.current && this.pfSide.current.children.length === 0) this.buildPlatform();\n\n    const span = track.offsetHeight - pin.offsetHeight;\n    const p = span > 0 ? this.cl(-track.getBoundingClientRect().top / span) : 0;\n\n    // 1 empty · 2 first line · 3 four windows · 4 second line · 5 platform · 6 the walk\n    const pA = this.seg(p, 0.04, 0.13);\n    const pWin = this.seg(p, 0.14, 0.34);\n    const pAOut = this.seg(p, 0.40, 0.47);\n    const pWinOut = this.seg(p, 0.42, 0.52);\n    const pB = this.seg(p, 0.46, 0.56);\n    const pPlat = this.seg(p, 0.54, 0.60);\n    const pWalk = this.seg(p, 0.60, 0.985);\n\n    this.revealWords(this.wordsA, pA);\n    this.revealWords(this.wordsB, pB);\n    if (this.titleA.current) this.titleA.current.style.opacity = String(1 - pAOut);\n    if (this.titleB.current) this.titleB.current.style.opacity = String(pB > 0 ? 1 : 0);\n\n    const fade = 1 - this.ease(pWinOut);\n    [this.w0, this.w1, this.w2, this.w3].forEach((r, i) => {\n      const arrive = this.ease(this.cl(pWin * 4 - i));\n      this.win(r, arrive * fade, 22);\n    });\n\n    this.win(this.plat, this.ease(pPlat), 26);\n    if (this.badgeA.current) this.badgeA.current.style.opacity = String(this.ease(this.seg(p, 0.30, 0.36)) * fade);\n\n    const n = this.screens.length;\n    const idx = Math.min(n - 1, Math.floor(pWalk * n));\n    if (pPlat > 0.2) {\n      if (idx !== this.at) this.showScreen(idx);\n    } else if (this.at !== 0) {\n      // rewind while the platform is still hidden, so the walk always opens on Pipeline\n      this.showScreen(0);\n    }\n  }\n\n  // ---- the platform's seven screens ---------------------------------------\n  buildPlatform() {\n    const side = this.pfSide.current;\n    if (!side) return;\n    side.textContent = ",
    ", s.crumb));\n    const head = this.el(",
    ", s.meta));\n    h.appendChild(head);\n\n    const tabs = this.el(",
    ");\n    s.tabs.forEach((t, i) => {\n      const on = i === 1;\n      tabs.appendChild(this.el(",
    "), t));\n    });\n    h.appendChild(tabs);\n\n    const tpl = s.cols.map((c, i) => (i === 0 ? ",
    ")).join(",
    " + s.align[i], c)));\n    h.appendChild(hd);\n\n    s.rows.forEach((r, ri) => {\n      const last = ri === s.rows.length - 1;\n      const row = this.el(",
    " +\n        (last ? ",
    "));\n      r.forEach((v, ci) => {\n        const isState = ci === r.length - 1;\n        row.appendChild(this.el(",
    " + s.align[ci] + ",
    " });\n  }\n\n  renderRail(s) {\n    const h = this.pfRail.current;\n    if (!h) return;\n    const r = s.rail;\n    h.textContent = ",
    ", r.head));\n    const card = this.el(",
    "));\n    const meta = this.el(",
    ", r.kvHead));\n    r.kv.forEach((kv, i) => {\n      const row = this.el(",
    "Flows to"
  ]
}